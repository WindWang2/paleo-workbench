# Goal Loop Ledger — V13 Geological Data Lineage Workspace & QGIS Compilation Control Plane

## CPP migration plan preparation — 2026-09-16

Goal: tag the reviewed migration plan, create three bounded goal-loop prompts and isolated worktrees. Oracle: committed plan/tag/worktree identities agree; each prompt has ownership, acceptance and resource limits; shared gate rejects concurrent/low-memory heavy work. Limit: 15 iterations. No migration implementation jobs are launched by this preparation task.

| Round | Change | Verification | Result | Next |
|---|---|---|---|---|
| 1 | Located and read installed goal-loop; retained complete skill in repository; wrote three prompts and shared contracts | Confirmed source baseline 671ee426, absent destination branches/tag, actual low free RAM | Preparation in progress | Validate executable resource gate |
| 2 | Added shared resource admission gate with 2-job cap, memory threshold and worktree-local build path | PowerShell parse PASS; actual low-memory rejection exit 75; actual exclusive-lock rejection exit 75; skill copy equal after newline normalization; all three prompt checks PASS | Passed | Commit plan and create tag/worktrees |
| 3 | Mock-command test exposed absent environment variable restored as empty string under PowerShell 7; fixed by removing only variables originally absent | First mock run failed on restoration assertion; no real build/test process was started | Fixed, pending recheck | Rerun resource contract checks |
| 4 | Reverified the focused environment-restoration fix and gate contracts | Mock build exit 23 propagated; jobs=2; environment restored; CTest zero-test guard; outside-build rejection; Configure arguments preserved: all PASS | Passed | Commit plan, prepare sparse worktrees, verify common lock |
| 5 | Staging the formerly untracked design exposed four Markdown trailing-space hard breaks; replaced with blockquote paragraph breaks | Staged diff check initially failed on exactly four lines | Fixed | Repeat staged check and commit |
| 6 | Prepared three sparse worktrees; explicitly populated their new empty indexes; documented safe initialization | All three worktrees clean; all three reject a main-repo shared lock with exit 75; after release all execute their memory probes; original composite_document.py SHA-256 unchanged | Passed | Seal local annotated tag and fast-forward clean worktrees to final plan commit |

---

## CPP-A 平台/QGIS 线（feat/cpp-platform-qgis）— 2026-09-16

Goal: 交付 C++20/Qt/QGIS 平台首轮实现（统一构建、C++ 画布/图层树、原生编辑与工具状态、布局导出、最小安装验证），以真实 QGIS smoke 和本轮全部 8 项 Oracle 为完成依据。上限 15 轮。工作目录 `C:/Users/wangj.KEVIN/projects/paleo-workbench-cpp-platform`。

| Round | Change | Verification | Result | Next |
|---|---|---|---|---|
| 1 | 启动侦察：读协议/总设计/CLAUDE/ADR0059/tool_context/tool_availability/stages/stage_profiles/layer_roles/map_stack_service(关键段)/edit_tools 接口；核验 worktree+branch+ancestor；fetch 后无新提交/PR/issue；探明工具链与 SDK | worktree=cpp-platform@d0347da2 ANCESTOR-OK；origin/main 仍 671ee426；Qt 6.8.0 前缀 C:\deps\Qt\6.8.0\msvc2022_64；QGIS 4.2.0 vendor install tree（主仓 qgis-vendor/output，qgis_core/gui/analysis .lib+.dll）+ 源码头 third_party/qgis；MSVC 14.38.33130+SDK 10.0.22621（reg.exe 黑名单→手工组环境）；cmake 3.27.2+ninja 在 VS 目录；空闲内存 3.96GiB<8GiB 门禁（暂不能编译） | 通过（仅侦察） | A0 文档四件套（内存低时先做纯源码/契约工作） |
| 2 | A0 四件套：00-baseline（ABI=Qt6.8.0/MSVC14.38/C++20、SDK manifest、生命周期决策）、01-contracts（Pwb::Qgis/ToolPolicy/Application targets、EditDeltaV1/StagedAsset、B/C adapter 接口、join key）、02-test-plan（platform.* 测试矩阵映射 Oracle）、03-progress | 文件存在且内容覆盖 prompt A0 全部要求项 | 通过 | A1：顶层 CMake/presets + 全部源码（等内存再编译） |
| 3 | 全量源码落盘：根 CMake/Presets/PwbQgisSdk.cmake；libs/tool_policy（评估器逐字移植）；libs/qgis（Runtime/MapSession/LayerAdapter/EditController/LayoutService）；libs/application；libs/ui；apps 主程序+--self-check；6+1 测试 + fixtures（GeoTIFF 已提交，GPKG 运行时生成）；Invoke-PlatformBuild.ps1；CI×2；04/05 文档。预编译自查修复 6 处 | 文件齐全；API 签名经 vendored 头核验（commitChanges/validateGeometry/SnappingMode/pageCollection/parallel job/getFeature/Capability 普通枚举） | 通过（未编译） | 等门禁内存 ≥8GiB |
| 4 | 跨实现契约准备：make_golden.py 用主仓 Python 评估器生成 28 上下文 × 77 工具 golden；新增 test_tool_policy_golden.cpp（QJson 对账 enabled/visible/checked/reason 全等）；PWB_TEST_SRC_DIR 编译期注入修 fixture 路径 | golden JSON 生成成功（28×77）；对账测试注册 | 通过（未编译） | 门禁 Probe ×3 均 75（4.5-5.1GiB）；诊断显示为桌面常态占用，长间隔等待 |
| 5 | Oracle 7 静态审查半程：逐条复核 C++ 评估器与 Python 的门序/判词/阶段语义/checked/severity（12 个高风险点，未发现语义偏差）；阶段性提交 28c610b6 | git log 确认；审查记录见本行 | 通过 | 长间隔等待内存（8+8 分钟两轮） |
| 6 | 资源等待结论：累计 7 次门禁 Probe（跨 ~35 分钟）全部 75（4.21–5.06 GiB < 8 GiB）；进程诊断确认占用为桌面应用常态（无本任务遗留进程）；独立轻量工作已穷尽 | Probe 输出留痕；无遗留构建/GUI 进程 | **未完成——外部资源阻塞**（编译/测试/Oracle 1-6/8 未执行） | 详见 ledger.md 与 03-progress.md 的恢复步骤；条件满足后从 Configure 继续 |

---

Branch: `feat/geological-data-lineage-qgis-control-v13` (base: origin/main @ 40531741)
Worktree: `C:\Users\wangj.KEVIN\projects\paleo-workbench-geodata-qgis-v13`

## Oracle（完成条件）

§33 Definition of Done 全部满足，且最终 PR 已创建（不合并）。可客观验证：
1. 分层测试（domain/data-manager/QGIS/cross-domain E2E）在本地通过；
2. §24 E2E 场景（import→bind→process→map→edit→commit→lineage→stale→recompute→update map→save/reopen）有真实测试通过；
3. `docs/development/geodata-qgis-control-v13/` 15 篇文档齐全；
4. branch 已 push、PR 已创建。

## 环境配方（本 worktree，已验证）

```bash
cd ../paleo-workbench-geodata-qgis-v13
export WT=C:/Users/wangj.KEVIN/projects/paleo-workbench-geodata-qgis-v13
# fast 腿
QT_QPA_PLATFORM=offscreen PYTHONPATH="$WT/native/qgis_render_bridge" \
  ../paleo-workbench/.venv/Scripts/python.exe -m pytest <files> -q --timeout=180 -p no:cacheprovider
# qgis 腿（额外）
PALEO_QGIS_BUILD_DIR="C:/Users/wangj.KEVIN/projects/paleo-workbench/native/qgis_render_bridge/build/qgis-vendor" ...
```
已复制主仓构建的 `qgis_render_bridge.cp312-win_amd64.pyd`（v0.7.0a0, 2026-09-15 18:13）到本 worktree native/ 以满足 conftest 桥源断言。若后续修改 C++ 需重建。

## 迭代记录

| 轮 | 改动 | 验证结果 | 判定 | 下一步 |
|---|---|---|---|---|
| 1 | 侦察：亲读 §5 全部 C++ 核/应用/qgis/tool_policy/平台与集成测试 + Python pipeline/models/interpolator/contouring/polygonization/service/test_geological_mapping_pipeline 等全文；subagent#1(explore) 扫 UI/mapping_workspace 17 文件；01-findings.md/01-decisions.md 落盘 | findings 按文件名索引覆盖 §5；worktree @35987e13 | 通过 | oracle 生成器 |
| 2 | oracle 生成器 tools/oracle/generate_map_pipeline_fixtures.py（真实 import + repair 恒等守卫），9→10 案例冻结（修 bool→int 序列化、partial-NaN 半径 3→6.5 使等值线可达） | 10 案例全部冻结成功，含失败文案/全NaN/常值场/#1150 | 通过 | runner 实现 |
| 3 | 实现 run_map_pipeline（libs/application，Qt-free）：extract→interpolate→contour→polygonize→GeoJSON 特征（Python 契约字段全量），grid/statistics/distance_policy 入 diagnostics | 编译通过 | 通过 | MainWindow 接线 |
| 4 | MainWindow「地质(&G) 地质因子图…」governed action（factor_workbench 复用）+ 对话框 + runGeologicalFactorMap（memory 图层×2、facts 角色注册、幂等重跑）；4 处 BEGIN CONV-01 CMake 块 | platform_map_pipeline 链接成功；首轮测试暴露 4 问题（弧度换算、错误返回契约、ogr provider 钉死内存 URI、测试断言用错案例）全部修复 | 修复后通过 | 测试绿×2 |
| 5 | platform.map_pipeline 绿 ×2 + 全套 43/43 ×2（含 mapping_kernel 8/8 ×2）；第2轮完整性审核补 2 缺口（#1150 diagnostics 断言、缺因子字段分支）→ 复绿 | ctest -R platform.map_pipeline ×2 全绿；ctest 全套 ×2 全绿 | 通过 | 三轮审核（#2 spec 对抗 / #3 Karpathy 并行） |
| 6 | 审核轮 #3（Karpathy，subagent#3，SHIP）：12 项发现全部为删除类，落实 9 项——错误通道合并（消除 *error 空指针 UB）、PUBLIC define 泄漏、max_neighbors 死字段、role_label_suffix/out_layer 死参、装饰性判空、setCurrentIndex(2) 本地化无关默认、9→10 注释、来源注释纠正、diagnostics 注释；generator np.bool_ 重复分支删除 | 修复后 ctest -R platform.map_pipeline 全绿 | 通过 | 审核轮 #2 |
| 7 | 审核轮 #2（spec 对抗，subagent#2，FIX-FIRST→已处理）：P1 is_closed 补 rel_tol（math.isclose abs 语义）、克里金 numpy 回退边界披露（对话框标注+PR 声明）、projected-CRS 案例（EPSG:3857 双方投影例外表，EPSG:32650 属 M7 无 pyproj 诚实分歧不入 oracle）、findings 补 geometry_units.py、guard 死代码删除；P0 账本/commit 项由收尾轮处理 | 11 案例重新冻结；platform.map_pipeline ×2 绿；mapping_kernel 8/8 ×2 绿；全套 43/43 ×2 绿 | 通过 | commit/push/PR |
| 8 | 收尾：§8 Oracle 逐项终验；commit（feat(app): wire mapping_kernel into MainWindow geological-factor map action）+ push + PR #1311；账本自审段落 | ctest -R 'platform.map_pipeline' ×2 = 1/1, 1/1；ctest -R 'mapping_kernel' ×2 = 8/8, 8/8；全套 ×2 = 43/43, 43/43；gh pr view 有 URL | 通过（全部 Oracle 满足） | 完结 |
| 1 | Phase 0 侦察：fetch、审 PR/issues、建 worktree、5 路并行只读审计、测试配方验证（fast 11 passed / qgis 4 passed） | 审计报告完成；环境可用 | 通过 | 写 baseline/overlap 文档，进入 Phase 1 |
| 2 | Phase 1 domain contracts：manual_edit run / 绑定字段 / source_usage / stage 状态 / intermediate policy / order 契约（19+4 测试）；邻接回归 70 passed + qgis 20 passed | 全绿（commits 7690c340/77a53c0f） | 通过 | Phase 2 UI |
| 3 | Phase 2/3：IngestPlanDialog + impact 门 + Inspector 用途行 + factor/fusion pin + harness 6 actions + Inspector 数据来源行；16+6+7+3 测试 | 全绿（010dfe13/63912df3/后续） | 通过 | E2E |
| 4 | Phase 4 E2E：§24 十八步全链；揭露并修复 3 个生产缺陷（producing_run_id 误读 / ref 缺 version_number / version.id 误读 → 真 stale 被 CURRENT 掩盖） | E2E 42/42（f03f81f7） | 通过 | 文档 + 审查 |
| 5 | Phase 5 文档 00-14；Phase 6 对抗性审查（子 agent 11 项发现）全部修复（含 as_new_version 丢失 / teardown UB / legacy 行键错 / RUNNING run 泄漏 / fail-open 门） | V13 全套 29+ 测试复验绿（18360589） | 通过 | 全量回归 + 15-verification + push + PR |

## 环境备忘（补充）

- worktree submodule：geo-viz-engine/well-log-engine 经本地 --reference 克隆填充（直连 GitHub shallow clone 会 EOF）；third_party gdal/proj 未初始化（测试不需要）。
- vendor runtime 经 junction：`native/qgis_render_bridge/build/qgis-vendor` → 主仓同路径（mklink /J），默认解析即可用，无需 PALEO_QGIS_BUILD_DIR。
- 后台 pytest 与前台 pytest 并行会互相拖慢并可能触发 timeout 假阳性——全量回归必须独占运行。


---

# CPP-B 数据/工程线 goal-loop 区段（feat/cpp-data-project，2026-09-16 启动）

目标与 Oracle 见 `docs/development/cpp-data/`（Prompt B，总设计 P0/P2/P3 持久化、CPP-20/309/601/602）。迭代上限 15 轮；三线共享重型槽（Invoke-ResourceGate.ps1，8 GiB 门槛，2 jobs）。

| 轮 | 改动 | 验证结果 | 判定 | 下一步 |
|---|---|---|---|---|
| B-1 | 侦察：协议/总设计/V13 文档/Python 模型（project/models+manager、catalog/models+db+storage+service 写路径、mapping_workspace/stage_state、paths）；worktree 复用核验（d0347da2，sparse OK，树干净）；工具链（VS2022 14.38 + VS 自带 cmake/ninja）；Python oracle（主仓 .venv 3.12.13 只读可用）；网络可用（sqlite.org 200）；内存 Probe=4.31 GiB → exit 75 | 关键语义已固化：.paleo.json（extra=allow 顶层、原子写+bak、5 路径节 relativize、project_root="."）；catalog.sqlite v5 canonical（16 表、WAL、sync_state 4 键）；artifacts 布局与 {stage}/{asset}/{version}/ 落盘；workspace dict 7 键 | 通过（轻量阶段，未需重型槽） | B0 文档 + 依赖落地（sqlite amalgamation、nlohmann 单头）→ B1 实现 |
| B-2 | B0 完成：4 份文档（baseline/contracts/schema-map/test-plan）+ oracle 管线（generate_fixtures.py 经真实 ProjectManager.save + DataCatalogService 产 8 fixture + manifest；dump_fixture.py 产 4 类 oracle dump）；vendored sqlite 3.45.1 + nlohmann 3.12.0 | fixture 落盘校验（typical: 2 asset/5 ver/1 run/2 tag + wc/lease/blob/members）；corrupt/future/missing 变体就位 | 通过 | C++ 主体实现 |
| B-3 | C++ 全量落盘：domain(ids/sha256/json/diagnostics)、project(schema spec 表+normalize+manager 原子写/恢复+paths)、workspace codec、catalog(sqlite RAII+repository+audit)、data_suite(contracts+CommitCoordinator+journal 恢复+facade)、pwb-inspect/migrate、9 个测试文件 + CMake | 未编译（内存门禁 75） | 未验证 | 门禁内 configure/build/test |
| B-4 | 静态自审两轮（无编译条件下的最大验证）：修复 UTF-8 路径桥、journal 恢复完整性（format/parents/size/ProjectSaved 分支/run+rebind 还原）、catalog 4 表漏加载、stage_states 种子、tmp 命名、.bak rename 语义、corrupt DB 分类、4 处测试结构错误；收尾文档 handoff.md + verification.md（含 6 次门禁探测台账） | 门禁 Probe ×6 全 75（free 4.2–4.7 GiB；Available 同级 → 机器真实吃紧）；按协议不绕过/不轮询 | 未通过（外部条件：可用内存） | 长等待后末次探测；仍 75 → 记录未完成状态并暂停 |

---

## 任务区段：CPP-C 算法/工作流/科学可视化（feat/cpp-science-viz，2026-09-16 启动）

目标与 Oracle：主仓 docs/development/full-cpp-migration-v1/04-prompt-science-viz.md（8 项全选）。上限 15 轮。

| 轮 | 改动 | 验证结果 | 判定 | 下一步 |
|---|---|---|---|---|
| 1 | 启动+侦察：读协议/总设计/CLAUDE；worktree 复用核验（cpp-migration-plan-v1 为祖先、工作区干净）；子模块按 gitlink 本地克隆初始化（geo-viz-engine@08851951、well-log-engine@f845e7ab，WLE 主仓为 shallow 改直克隆）；审 A 线 contracts（无 ABI manifest→viewer 用 venv Qt 6.8.0 测试配置）；写 C0 四文档（baseline/migration-matrix/contracts/test-plan），冻结 C1 算法=seismic.coherence_c3（生产链 providers→KERNELS['c3']→geoviz_seismic）与容差 | 远端无新提交；工具链核验（MSVC 14.38+VS CMake/Ninja）；内存 4.17 GiB<8 门禁→先做独立源码工作；tiny.sgy/A1.Las fixture 可用，geoviz loader 只读 oracle 验证通过（8×8×32 dt=2ms） | 通过（C0 交付） | C1：libs/algorithms + libs/workflow 编码 |
| 2 | C1–C4 全部源码落地：pwb::science SDK（types/outcome/algorithm/publisher/registry）+ coherence_c3 移植（reflect 填充/奇数窗口钳制/float32 幂迭代/NaN→1.0 语义对齐 oracle）+ pwb::workflow task runtime（单 worker、queued→running→{succeeded/failed/cancelled}，取消只 request_stop 终态由 execute 决定）+ pwb::viz（SelectionEventV1、地震数据源接口、in-memory/owning backend、map_slice_to_indexed8）+ science_suite CMake 入口（viewer opt-in fail-closed）+ 4 个 Qt-free 测试 + viewer 测试 + WLE 宿主 adapter + 2 个 oracle 生成脚本；冻结 fixture 已生成（coherence 7 case 含真实 tiny.sgy ×2 参数组、seismic crossline-major 置换 + 三轴 expected + indexed8） | MSVC /Zs 语法检查核心 4 文件+测试 4 文件全部 EXIT=0；O6 静态 grep：核心无 Python/Qt Widgets/QGIS 代码引用（仅 2 处注释文字）、无第二 GIS 栈；oracle 生成器实测发现并修正 iline 1-based 值语义与常量体非精确 1.0（0.99999976）两点，baseline 文档同步 | 通过（源码级）；构建待门禁 | 内存 4.78<8 GiB 被拒 1 次 → 写 verification/handoff 骨架后再探；随后门禁下 configure/build/ctest G1-G4 |
| 3 | 资源等待与阻断处理：门禁 Probe 4 次 exit 75（4.17/4.78/4.84/4.52 GiB）；只读诊断确认 Available≈Free≈4.5 GiB（桌面应用稳态占用 ~26 GiB：ZCode×4/ChatGPT/WXWork/Feissu/opencode/WSL，无编译进程，非门禁度量缺陷）；自审查修复 3 处（task_runtime 持锁发布×2、析构语义）；完成 handoff.md + verification.md（O6 已验证、O1-O5/O7 构建侧如实标 blocked-on-memory）；提交 013d9354；长间隔重探（4min/7min 后仍 4.35 GiB） | 独立源码/契约/文档工作全部完成并提交；MSVC /Zs 全绿；oracle fixture 已入库；仅剩必须过 ≥8 GiB 门禁的 build/ctest | 未通过（外部资源条件阻塞，非代码缺陷） | 需要用户释放内存至 ≥8 GiB（关闭部分桌面应用）或明确授权降低小目标构建门限；随后按 verification.md §5 序列执行并回填 |
| 4 | 终局探测：累计等待 ~25 分钟（4/7/10 分钟间隔）后第 5 次 Probe 仍 exit 75（4.22 GiB） | 独立工作已穷尽（源码/契约/文档/fixture/静态验证全部完成并提交 013d9354、8ce51843）；阻塞项仅为必须过门禁的 build/ctest（O1/O2-C++侧/O3-ctest/O4/O7）与构建后回填 | 未通过（持续外部资源阻塞） | <loop-pause>：需用户释放内存至 ≥8 GiB（关闭部分桌面应用，如 WXWork/飞书/ChatGPT/空闲 ZCode 会话/WSL）或明确授权对小型核心构建降低门限；随后执行 verification.md §5 序列并回填实测数字 |

---

# CPP-D core-verify goal-loop (feat/cpp-core-verify, 2026-09-16 启动)

目标:已合入 main 的 Qt-free C++ 核心在本机真实编译+CTest 全绿。Oracle: configure/build 退出码 0;data.* 与 science Qt-free 组零失败(关键组两遍)。上限 15 轮。QGIS/Qt-linked 需 8GiB 门禁,记为遗留。

| 轮 | 改动 | 验证结果 | 判定 | 下一步 |
|---|---|---|---|---|
| R1 | 建分支+VS2022/MSVC19.38 configure data_suite | tests/cpp/data 不存在,configure incomplete | 未通过 | 关测试先验库 |
| R2 | -DBUILD_TESTING=OFF 重配 | Configuring done | 通过 | 编译 |
| R3 | 首编 | json.hpp 为多头 stub,C1083 | 未通过 | 换真 amalgamation |
| R4 | 官网下 3.12.0 真单头(953436B)替换 vendored | C1083 消失,转出 project/workspace 真错误 | 通过(本项) | 修头引入链 |
| R5 | errors.hpp 补 stdexcept/string_view;document/schema 补 errors 引入+Json 可见;manager 补 utility;state.cpp 两处自比 end+遮蔽修复 | domain/project/workspace 绿 | 通过 | 修 catalog |
| R6 | sqlite.cpp 补 Json using 未中;真因 sqlite.hpp 缺 json.hpp 引入,补之 | catalog 绿 | 通过 | 补 pwb/data 头 |
| R7 | 新建 pwb/data/facade.hpp+commit_coordinator.hpp(按双 .cpp 用法+contracts 反推,现存 API 全存在);catalog sqlite 私有引入转 PUBLIC | 5lib+pwb_data 绿,工具报缺 contracts.hpp | 通过(本项) | 写 contracts.hpp |
| R8 | 新建 pwb/data/contracts.hpp(伞头+v1 冻结断言) | 5lib+2exe 全绿零 error | 通过 | 写 tests/cpp/data,开测试 |
| R9 | oracle 管线重跑:8 fixture+manifest(2资5版1run)+8x4 dump 全落盘 | typical catalog 2/5/1/2 与 B-2 一致 | 通过 | 写 harness+首测 |
| R10 | 新建 tests/cpp/data:harness/compare/CMake+domain_ids/hygiene;修 StrongId 字面歧义/diagnostics 引入/宏空格/cmd 引号三坑 | ctest 2/2 两遍全绿 | 通过 | fixture 系测试 |
| R11 | workspace_codec 7用例(回落/种子/maturity/三态/extra/往返/绑定过滤;实现用effective-kind,修正预期) | ctest 3/3 两遍绿 | 通过 | 读路径系测试 |
| R12 | diagnostics/catalog_read/project_paths 三件套(损坏/未来/缺资源/读库/计数/逃逸/零写入;修u8字面量与C:相对路径两坑) | ctest 6/6 两遍绿 | 通过 | 写路径系测试 |
| R13 | 写路径7件套(roundtrip/recovery/catalog_write/commit/commit_recovery/cli x2);修rename-open-handle真崩溃+null-value抛错+ver_前缀;test_main去缓冲 | ctest 13/13 两遍绿 | 通过 | oracle_compare |
| R14 | oracle_compare(树/11表行投影/resolve;修comparator符号差异+const-operator[]抛错+表主键;派生表显式记边界) | ctest 14/14 两遍绿 | 通过 | science_suite |
| R15 | science Qt-free:补3×/utf-8;补coherence缺失右括号+make_coherence_c3定义;修测试命名空间/shared_ptr/span字面量/轴越界/buffer尺寸 | 库绿,测4错 | 通过 | 消测试错 |
| R16 | param整数收编integral-double(pydantic-lax);修slice测试3处+owning所有权被构造踩掉真bug+indexed8按f32读 | science 4/4 两遍绿 | 通过 | 提交收口 |

---

# 任务区段：01 — mapping_kernel 接入 C++ MainWindow（feat/cpp-conv-01-app-map-pipeline，2026-09-17）

目标：MainWindow 增加「地质因子图」动作：读井点 → mapping_kernel 插值/等值线/多边形化 → QgsVectorLayer 上图；`platform.map_pipeline` 本地绿 ×2 后开 PR。迭代上限 200；subagent 配额 3；禁止等待线上 CI。

| 轮 | 改动 | 验证结果 | 判定 | 下一步 |
|---|---|---|---|---|

### 开发量自审（§8.9）

| 项 | 数量 | 说明 |
|---|---|---|
| 亲读文件数 | ≈60 | §5 C++ 核 14（7 hpp + 7 cpp）、应用层 6、qgis 3、tool_policy 7、tool_actions 2、平台测试 10 + 2 头、集成测试 3、CMake 5；Python 13（pipeline/models/interpolator/contouring/polygonization/geometry_units/service/test_geological_mapping_pipeline/test_factor_interpolation/test_factor_grid_pipeline/test_e2e_factor_map_contract/test_native_factor_map/factor_grid_result 精读相关段）；文档 8（main-plan/entry-switch/0057/0059×3/v5 00-07）；另 subagent#1 只读扫描 UI/mapping_workspace/测试 17 文件（报告全文并入 findings） |
| oracle 案例数 | 11 | 全部由真实 Python 产品输出冻结（生成器 import 真模块；无手写期望值），含 1 失败文案案例 + 全 NaN/部分 NaN/常值场/投影 CRS/#1150 边界 |
| oracle 对账特征数 | ≥42 | 每次运行输出 "11 cases, 42 features compared" |
| 审核轮次 | 3 | ①正确性（对 Python，自查+抽查脚本，抓出弧度换算/错误契约/ogr 钉死/断言错配 4 缺陷）②完整性（补 #1150 diagnostics 断言 + 缺因子字段分支）③Karpathy(subagent#3, 12 项落实 9) + spec 对抗(subagent#2, P1×2 落实/边界披露/案例补充) |
| 账本轮次 | 7 | 本表 |
| subagent 配额 | 3/3 | #1 explore / #2 spec 审计 / #3 Karpathy 审核 |
# GOAL Loop Ledger — cpp-conv-02 (MapDocument / Composer 纯数据核)

目标：把 Python MapDocument / MapCompositionDocument 的数据契约迁到 C++ Qt-free 库 `libs/mapping_document`，JSON round-trip 与 Python oracle 对账。
完成条件：本 prompt §8（账本轮次≥实现+3审核、findings 全覆盖、decisions、`mapping_document.roundtrip` ctest ×2 绿、mapping_kernel 若编则全绿 ×2、无 CI 依赖、commit+push+PR）。

| 轮 | 改动 | 验证 | 判定 | 下一步 |
|---|---|---|---|---|
| 1 | worktree 建立 `35987e13`（origin/main），分支 feat/cpp-conv-02-map-document；账本段初始化 | worktree list / rev-parse 确认 | 通过 | 通读 §5 源码 |
| 2 | 通读 §5 全部源码/测试/文档（layers 716、composer 2993、tests 4040、docs 972、layout_service/json.hpp/mapping_kernel 约定）；写 02-findings.md（按文件索引逐符号） | findings 覆盖 §5 每个文件 | 通过 | 决策记录 |
| 3 | 写 02-decisions.md（D-01..D-19：范围、读侧契约、family 表、extras、falsy 语义、round 复刻等） | 19 条决策落盘 | 通过 | oracle fixture |
| 4 | tools/oracle/generate_map_document_fixtures.py：真实 import 产品码，冻结 22 文档案例+14 组合案例+8 像素案例（共 44）；生成器自断言（幂等/≥30） | 生成 198KB fixture，抽查形态正确 | 通过 | C++ 核实现 |
| 5 | 实现 libs/mapping_document（2 头文件+2 编译单元+python_compat）+ CMake BEGIN CONV-02 + mapping_document.roundtrip 测试 | 编译通过；首跑 43 失败：int/unsigned 类型差 | 未通过 | 整数写出形态修复 |
| 6 | py_int_json（非负→uint64，负→int64，复刻 Python 整数在 nlohmann 的解析形态）；fixture 两处期望缺陷修正（schema 规范化、add_element arg 与真实对象一致） | PASS: 0 failure(s) over 106 checks | 通过 | ctest ×2 |
| 7 | ctest -R 'mapping_document.roundtrip\|mapping_kernel.' ×2：9/9 全绿 ×2（8 kernel + 1 roundtrip） | 100% tests passed ×2 | 通过 | 三轮审核 |
| 8 | 审核轮1（正确性）：fixture 再生确定性修复（layers.uuid4 生成器侧顺序桩）；两次生成字节一致；106 检查复绿；抽查 falsy/carrier/pixels 与 Python 行为一致 | FIXTURE BYTE-DETERMINISTIC + PASS 106 checks | 通过 | 审核轮2（对抗 spec） |
| 9 | 审核轮2（对抗 spec，subagent #1/3）：修 P1-1 像素平局钉死（13.97/24.13@150 → 82/142）、P1-2 input_version_ids 改 verbatim 种子（返回 Json）、P1-3 probes 全部改活对象计算 + cpp-read-contract 三分标注、P1-4 findings reorder 更正、P2：24 值注释/0x 拒收/溢出钳制/reorder 重复 id 对齐 dict 语义/死三目清理；新增 2 组合案例 + 2 像素案例（共 48） | 143 checks PASS；fixture 再生字节一致 | 通过 | 审核轮3（Karpathy） |
| 10 | 审核轮3（Karpathy/代码质量，subagent #2/3）：修 P1 全部（find_layer 保留并在测试中行使、*_or helpers 下沉 composition.cpp、collect_extras 收敛 4 处、remove_layer 返回值冻结进 fixture "returns" 并断言）+ P2 十八项（去 _Model 后缀、删 operator==/死 using/errno/恒真分支、paper 查表单次大写、is_vector_family/is_known_element_type 移入 .cpp、溢出钳制注释更正、探针带 element_id、像素轴独立断言保留等） | 重建 + 151 checks PASS；9/9 ×2 绿；fixture 再生字节一致 | 通过 | PR |
| 11 | commit f0d150a9（信息按 §8.7）+ push -u origin + gh pr create → #1315；§8 验收命令原文复跑：mapping_document.roundtrip ×2 Passed，mapping_kernel.* 8/8 Passed | 100% tests passed ×2；PR URL 落账 | 通过 | §8 自审 |
| 12 | §8 完成条件自审（见下）| 全项满足 | 通过 | 完成回合 |

## §8 自审（审计段）
- 读过的文件数：§5 清单 24/24 全文通读（layers.py、composer/ 7 个 .py、document_io.py、map_document_snapshot.py、map_styles.py、layout_export.py、geological_pipeline/templates.py+pipeline.py、12 个相关 pytest、qgis-cartography-runtime-v11/ 14 篇、layout_service.cpp、json.hpp + support.cpp、mapping_kernel CMake/测试/生成器约定、根 CMakeLists）——无缺项。
- oracle 案例数：48（22 map-document + 16 composition + 10 pixel）≥ 30；生成器真实 import 产品码，字节级再生确定。
- 审核轮次：3（正确性自审 / 对抗性 spec 审核 subagent#1 / Karpathy 质量审核 subagent#2），subagent 用量 2/3。
- 账本轮次：12 轮（含本轮）≥ 实现 6 轮 + 3 审核。
- ctest 证据：`mapping_document.roundtrip` ×2 Passed；`mapping_kernel.*` 8/8 ×2 Passed（本 worktree 编了 mapping_kernel，未改动其任何文件）。
- 无新增 workflow；未等待线上 CI；PR #1315 已建（含用户流程/案例数/本地证据/不依赖线上 CI 声明）；未 merge、未 force-push。
# CONV-03 等值线/相带 GeoJSON 图层产物（feat/cpp-conv-03-layer-geojson，2026-09-17 启动）

目标：把 generate_contour_layer / generate_facies_polygon_layer 的要素打包（geometry+properties，无样式 UI）迁到 mapping_kernel 新文件 layer_products.*，对 Python 属性键与 round(area) 语义对账。Oracle：§8 共 9 条（ctest 新目标 ×2 + 既有 mapping_kernel.* ×2 + 账本 + findings/decisions + commit/push/PR）。上限 200 轮。worktree /home/kevin/projects/paleo_project/worktrees/cpp-conv-03-layer-geojson，基线 35987e13。

| 轮 | 改动 | 验证结果 | 判定 | 下一步 |
|---|---|---|---|---|
| 1 | 侦察：读 goal-loop/karpathy SKILL、总计划、CLAUDE、domain/issue-tracker 文档；建 worktree（BASE=35987e13）；全文阅读 §5 全部源文件（contouring.py/polygonization.py/layers.py/color_ramps.py/factor_layer_products.py/两个 C++ 头/两个 C++ 实现/pipeline 测试+8 个命中 pytest 文件+geometry_units/topology/geometry_operations/geometry_planar/map_styles/factor_grid_result/crs_policy/json.hpp）；写 03-findings.md（逐符号+语义清单+oracle 案例表） | findings 覆盖 §5 每个文件；识别三个移植陷阱：Python % 负操作数语义、math.isclose rel+abs 双容差（is_closed/标签）、mean float32 累加语义；clip 硬依赖 shapely 确认不可精确复刻 | 通过 | 实测 mean/%g 语义 → 写 oracle 生成器 |
| 2 | oracle 生成器 `tools/oracle/generate_layer_product_fixtures.py`（真实 import generate_contour_layer/generate_facies_polygon_layer，repair 打 identity）：17 个网格（ramp/batch4/islands/donut/nested/spike/holed/flat/nan/inf/1x1/strip/neg/micro/macro/plain/moat/geo 三 CRS/checker）×24 contour + 26 facies 案例 + mean_units（numpy f32 均值 10 档尺寸）；fixture 落盘 | 生成成功：264 features、10 个早退分支案例、geographic area_approx_m2 案例；实测钉死两语义：np.mean(float32)=float32 累加、np.nanpercentile 输出 float64（C++ 既有 quantile 核可直接复用）；1x10 条带空产物原因核实（linspace(0,10,1)→[0]，ymax 塌缩） | 通过 | 实现 C++ 产物层 |
| 3 | 实现 layer_products.hpp/.cpp（复用 marching_squares_contours/classify_grid/polygonize_class/filter_small_polygons/quantile/nice 核；自补 python_mod、isclose abs_tol 变体、%g、area_unit_label、ring_area_with_unit、f32 pairwise mean）+ 测试 + CMake CONV-03 块（根+kernel+tests） | 编译通过（补 crs_policy include）；首跑 ctest 314 失败：两类系统性问题——fixture 漏 "type" 键、比较器顶层 int(5) vs unsigned(6) 类型枚举差异 | 未通过 | 修 fixture 与比较语义 |
| 4 | fixture 补 "type"；测试侧 canonical()=dump→parse 规范化（线格式相同、仅内存类型枚举不同，不改 libs/domain） | 重生成+重跑：314→1 失败，仅剩 contour_nested12_nice 的 properties.length 差 1 ulp | 未通过（1 项） | 逐位对账 length |
| 5 | 定位：CPython math.hypot 不调 libm（vector_norm：幂次缩放+VD 平方+fma+Neumaier 补偿和+根号微分修正）；layer_products 本地实现 python_hypot/python_polyline_length（不动已绿 contouring.cpp）；修正 max 遇 NaN/inf 的选择语义；standalone fuzz 30 万对（随机/极端量级/次正规/特殊值/等长段）0 失败 | `ctest -R mapping_kernel.layer_products` ×2 全绿；随后全量 `ctest -R mapping_kernel` 9/9 ×2 全绿 | 通过 | 三轮审核（正确性/完整性/Karpathy） |
| 6 | 审核轮 1（正确性，subagent 对抗审核 #1）：对照 Python 逐分支审 C++，报 2 个 P1 + 6 个 P2（含 2 个豁免项：kernel 双 length 实现、pyproj 未知 id 限界，均已入 decisions） | P1-1 facies 早退未回显 min_area（Python "small_polygon_threshold": min_area 逐字）→ 修复 + 新钉案例 facies_nan6_minarea；P1-2 interval 阶梯 Python math.ceil 返回 int 使 0*interval=+0.0，C++ std::ceil 产 -0.0（标签 "-0"）→ 规范化 + 新钉案例 contour_neghalf4_interval；59 案例 ctest 全绿 | 通过 | 审核轮 2（完整性） |
| 7 | 审核轮 2（完整性，自查）：Python 分支矩阵逐项对 fixture——发现 6 个缺口：simplify>0、interval≤0（falls through 但 contour_interval 仍记录）、显式空 levels、names 少于 thresholds+1（类 id cap）、显式 colors、洞升级计数无 >0 案例 | 补 6 案例（contour 3 + facies 3）；promote 猎取 400 种子无命中（与 polygonization oracle 同状），fallback 帧+孤块案例冻结 qc 管道；59→59 案例（29 contour + 30 facies，285 features）ctest 全绿 ×2 | 通过 | 审核轮 3（Karpathy） |
| 8 | 审核轮 3（Karpathy，subagent #2）：裁定 SHIP 无 P1；2 个 P2 清理执行——删除 fixture 生成器中重复的 contour_neghalf4_interval 案例（计数 29→28）+ 把未在外部使用的 area_unit_label/layer_crs_is_geographic 从公开头收进匿名命名空间（float32_mask_mean 因测试直调保留公开）；顺手 P3：kPalette 用 std::size、删生成器死行、float32_mask_mean 空守卫加契约注释、合并双匿名命名空间 | 重生成 58 案例（28 contour + 30 facies，282 features）；全量 cmake build + `ctest -R mapping_kernel` 9/9 全绿 | 通过 | 终验 ×2 + PR 文档 |
| 9 | 终验与收口：最终 diff 审计（12 文件，全部在允许写入范围内，.github 0 改动，既有核源文件 0 字节 diff）；PR #1312 创建（正文含用户流程、oracle 案例数 58+10、本地 ctest 证据、明确"不依赖线上 CI"）；未 merge、未等待 Checks | §8 九项完成条件逐项核销（见最终回复审计段）；worktree 干净 | 通过 | 任务完成 |
## CPP conversion task 04 — ring clip & repair (2026-09-17, worktree cpp-conv-04-ring-clip-repair)

Goal: port the shapely-free subset of clip_polygon_to_ring / clip_polyline_to_ring / repair_invalid_geometry (closure, orient, bounded self-intersection split) into libs/mapping_kernel ring_ops; freeze portable oracle cases, skip shapely-only cases explicitly. Limit: 200 iterations, ≤3 subagents.

| Round | Change | Verification | Result | Next |
|---|---|---|---|---|
| 1 | Read all §5 sources in full (7 py modules, 10 test files, 9 geotopo-editor docs, ADR 0044/0059, C++ contouring stitch + polygonization hole vote read-only) | findings written per public symbol | pass | findings/decisions |
| 2 | 04-findings.md (per-symbol facts + portable/shapely table) + 04-decisions.md (D1–D10) | files on disk | pass | implement kernel |
| 3 | ring_ops.hpp/.cpp: closure dict pass, signed-area orient, single-proper-crossing bowtie split, boundary-arc overlay clip, polyline clip | g++ -fsyntax-only | pass | oracle generator |
| 4 | generate_ring_ops_fixtures.py (real product code, input-shape classification, canonical freeze); empirical corrections: 3-coord ring → make_valid makes a LineString (not ValueError); bowtie net area 0 is not degenerate | fixture written | pass (classifier fixed twice) | C++ test |
| 5 | ring_ops_test.cpp (mirrored canonicalization) + CMake BEGIN CONV-04 blocks + first run | 15 failures | fail | fix per finding |
| 6 | Fixes: canon re-rotate after direction reversal (both sides), cross-part overlap checks for MultiPolygon repair (both sides), split_boundary wrap-arc steps formula, polyline split dropped intermediate vertices | ctest ring_ops 34+9 green | pass | full suite ×2 |
| 7 | Full mapping_kernel suite ×2 | 9/9 ×2 (ring_ops included) | pass | reviews |
| 8 | Review round 1 (completeness, self): 5 uncovered branches found → added cases; fixture 48 = 34 portable + 14 optional, green | ctest ring_ops 100% | pass (5 gaps fixed) | subagent reviews |
| 9 | Review round 2 (Karpathy, subagent): P1-1 clobbered accumulated ledger (restored + appended instead); P2 dedupe ×3 (orient/make_polygon, parts-disjoint helper, dead code), clip empty-subject geom_type "" ⇔ None, classifier honesty for <2-vertex line, decisions D6 CMake wording, test failure detail | applied; green rerun | pass (1×P1, 8×P2 addressed) | spec review |
| 10 | Review round 3 (adversarial spec, subagent): oracle regeneration byte-equal 48/48, 7 portable cases re-derived against raw Python (incl. 8/3,4/3 bowtie crossing), mutation testing 4/4 caught. Findings: F1(P1) self-intersecting line classified portable but C++ returns 1 piece vs GEOS 3; F2 multi unclosed 3-coord ring over-rejected; F3 ValueError→GEOSException wording; F4 zero-length line over-rejection (kept, documented); F5 orient direction not pinned by canonical comparison; F6 case name mismatch | applied F1–F6 | pass after fixes | final suite |
| 11 | Fixes for rounds 2–3: line self-intersection rejected in classifier + kernel (+2 optional pins), multi 3-coord unclosed ring proceeds to orient (+1 portable pin), ring_status area on implicitly closed rings (both languages consistent), raw-direction CCW/CW assertions for repair, reason-detail on geometry failures, docs wording. Fixture 51 = 35 portable + 16 optional | ctest ring_ops 100% ×2; full mapping_kernel 9/9 ×2 | pass | audit + PR |
# CONV-05 constrained-IDW 核 goal-loop（feat/cpp-conv-05-constrained-idw，2026-09-17 启动）

目标：把 vendored Haiyou constrained-IDW 的数值核迁到 C++（libs/mapping_kernel，Qt-free），
用 Python 适配器同一 fixture 冻 oracle；断层约束与各向异性按 Python 契约。
完成条件：prompt §8（ctest `mapping_kernel.constrained_idw` 绿 ×2、既有 mapping_kernel.* 绿 ×2、
findings/decisions 账本齐、commit/push/PR）。subagent 配额 ≤3。禁止等线上 CI。

| 轮 | 改动 | 验证结果 | 判定 | 下一步 |
|---|---|---|---|---|
| 1 | 建 worktree（BASE=35987e13）；全文读 §5：adapter 721 行、constrained_engine 8826、fast_grid 704、direction_corridor 1487、masks 256、performance 484、interpolator 1009、constraints 254、测试×7（algorithm/parity/integration/diagnostics/p2_perf/routing/capabilities）、AUDIT_MATRIX、v6-06、ATTRIBUTION；核对 mapping_kernel C++ 既有约定（CMake/tests/fixtures/oracle 生成器） | 数值核=generate_constrained_idw 表面路径；等值线链在 grid_z 定型之后（1332–1707 行顺序）；scipy 依赖仅 distance_transform_edt 进核（2 处）；GEOS/pyproj/CV-R² 为胶水 | PASS | 写 findings/decisions |
| 2 | 05-findings.md（逐符号）+ 05-decisions.md（D1–D14）落盘 | 文档存在且覆盖 §5 全部文件 | PASS | oracle 生成器+fixture |
| 3 | oracle 生成器 tools/oracle/generate_constrained_idw_fixtures.py（spy 捕获 adapter 真实引擎输入）+ 冻结 11 案例/16,025 格/54 诊断键 fixture | 生成器跑通；engine_surface_only 与 adapter 网格 bit 相等证实 D3 | PASS | C++ 实现 |
| 4 | constrained_idw.hpp/.cpp（引擎级契约：几何谓词/掩膜/EDT/方向走廊/三路插值核/补洞/平滑/锚定/along-track/generate 主入口）+ 测试 + CMake CONV-05 三处追加 | 编译通过；首轮 116 失败 | FAIL（预期）| 阶段对拍调试 |
| 5 | 阶段级对拍（C++/Python 各段转储）：修 EDT 提取覆写 bug（分离输出缓冲）、bfs_reach 补集语义（EDT(~seeds)）、锚定三计数器、blank 诊断 else 支、测试 config 读取缺字段；再修 oracle seam：spy 被 CV 4 折调用覆盖→只捕获表面 pass，重生成 fixture | plain/全部案例阶段差 ≤1.8e-15；ctest 100% | PASS | 清理+×2 |
| 6 | 移除调试插桩（转储/env 过滤/临时脚本）；重编 | mapping_kernel.* 9/9 绿 ×2（含新 constrained_idw） | PASS | 审核轮 ×3 |
| 7 | 审核轮 1（正确性，subagent 对拍 Python 逐函数）：确认 exact-hit/排序/权重/EDT/掩膜/走廊全等；报 2 P1（int(round) banker's、锚定 5×5 窗口）+ hull 空栅格 + blocked 集合计数 + strip/护栏等 P2 | 修复全部落地 | PASS | 审核轮 2 |
| 8 | 审核轮 2（spec 完整性，subagent）：发现 P0——测试 compare_grid 计算容差却未断言（容差断言形同虚设）；第三错误分支（边界面缺少有效顶点）无 oracle 案例；5 个 adapter 标量未断言 | P0 修复（worst diff/allowed ≤ 1 真断言）；新增 engine_error_empty_exterior 案例；补 n_break/n_direction/search/decluster 断言（decluster 按方向线分支） | PASS | 审核轮 3 |
| 9 | 审核轮 3（Karpathy 质量，subagent）：死代码 dir_weight_base/auto_direction_radius、M_PI、<cctype>/<limits> 头、D14 命名漂移、注释清理 | 全部清理；D3 的 C++ 侧同义反复检查移除（跨语言证据由各自冻结期望承载）；测试 finite_mean 升级为完整 numpy pairwise 递归 | PASS | 回归 ×2 |
| 10 | 修复后全量回归：fixture 重生成（12 案例），ctest constrained_idw ×2 + mapping_kernel.* 9/9 ×2 | 全绿（checks=591, failures=0） | PASS | 提交/PR |
# CONV-06 WorkflowSpec DAG 纯模型（feat/cpp-conv-06-workflow-spec，2026-09-18 启动）

目标：把 workflow/dag/model.py 与 validation.py 的纯数据结构 + 校验迁到 C++，JSON round-trip 与非法 spec 报错文案对账。
Oracle：§8（findings 覆盖 §5、decisions 记录、ctest workflow_spec.validate 绿×2、mapping_kernel 仍绿×2、commit+push+PR）。
上限 200 轮；subagent 配额 3。

| 轮 | 改动 | 验证结果 | 通过 | 下一步 |
|---|---|---|---|---|
| R1 | 通读 §5 全部原文：dag/{model,validation,plan_view,__init__}.py、recipe.py、dependency_graph.py、orchestrator.py、contracts/ 6 文件、harness/{spec,validation,registry}.py、providers/execution.py(validate_parameters)、6 个测试文件全文、scientific-workflow-v8 5 篇、task_runtime.hpp、engine.py 关键段；确认 Pwb::Domain(ordered_json+Sha256) 可复用、python3 可 import 真实模块 | 文件读取清单齐；关键语义入 findings（Kahn leftover 含环下游、消息 py-repr、validate_parameters 消息格式） | 通过 | R2 写 findings/decisions |
| R2 | 写 06-findings.md（model/validation 逐符号、其余 §5 文件覆盖、测试→oracle 翻译表、缺口清单）+ 06-decisions.md（D1-D10：范围、ActionCatalog、ordered_json、py-repr、强转、Kahn leftover、BindEnv、create、布局、oracle 组织） | 两文件落盘，findings 覆盖 §5 全部文件索引 | 通过 | R3 oracle 生成器 |
| R3 | 写 tools/oracle/generate_workflow_spec_fixtures.py（import 真实 model/validation/registry/validate_parameters，含 duck-registry 冻结 DESTRUCTIVE 分支）并冻结 fixture：合法 24 / 非法 45 / 条件树 13 / slot 18 / resolve 17 / hash 9 / run-roundtrip 3；抽查消息（cycle list、whitelist sorted、meta-workflow、multi-problem 顺序）逐字正确；发现并记录 duplicate-id→空环消息怪癖 | 生成器退出码 0；断言 valid≥20/invalid≥20 通过；消息抽查通过 | 通过 | R4 C++ model.hpp/cpp |
| R4 | 实现 libs/workflow_spec：model.hpp/cpp（7+5 枚举、RetryPolicy、NodeCondition(递归子用 0-or-1 vector 规避 optional 不完整类型)、NodeSpec/SlotSpec/WorkflowSpec/NodeRun/WorkflowRun、canonical_hash=排序键紧凑 dump+Sha256）、python_repr（py_repr/py_type_name/py_repr_double/强转）、validation.hpp/cpp（静态门全分支、Kahn、binding walk、resolve_value、slot_schema、validate_parameters 子集）、validate_test.cpp、CMake CONV-06 块 | 编译 3 处修正（is_dict→is_object、optional 递归→vector、ModelError include）；首轮 oracle 对账 226 checks 33 failures，定位三类根因：nlohmann 花括号初始化陷阱（{nullptr}→[null]）、union type 时顶层 object 检查误触发、to_dict 漏回填 action_status、drift 案例 fixture 语义设计错（生成器侧修正为「盘上缺节点条目」）；全部修复后 226 checks 0 failures | 通过 | R7 ctest ×2 + mapping_kernel ×2 |
| R5 | ctest 验证：workflow_spec.validate 两遍独立 Passed（#29, 0.01s）；全树 cmake build 无 error；全 ctest 29/29 两遍（含 8 个 mapping_kernel.* 全绿） | 两遍输出已留证据（100% tests passed out of 29 ×2） | 通过 | R6 审核轮 1 正确性 |
| R6 | 审核轮1（正确性）：自查发现 schema_version presence-wins 偏差并修复；subagent#1（对抗审核，1/3 配额）返回 1×P1+15×P2+13 条覆盖缺口。裁决：修 13 项（P1 空串真值、to_dict state/value 键序、正则 $ 尾换行、falsy 容器 or-语义、bool() 真值、state 非串抛、时间戳 int 保真、resolve key 非串 BindingError、enum bool 交叉相等、schema 污染 type/required/minItems/null-property、kind str() 强转、node_runs 重复 id dict 语义、py_repr 控制字符 \xNN）；记录 4 项为 D11 已声明偏差；补冻结案例（truthiness 4、newline、falsy raw、union 全未知、enum bool、minItems 2.0、null property、ref key 3、duplicate+cycle 组合、from_dict_error 5）；测试升级为字节级 dump 对比 | 259 checks 0 failures；ctest 绿 | 通过 | R7 审核轮2 完整性 |
| R7 | 审核轮2（完整性）：脚本化审计——从 Python 源提取 46 个错误消息形状逐一比对冻结案例，发现 2 个缺口（顶层 expected object、maxItems）即补；补 all_enum_states run round-trip（7 NodeState + 5 RunState 全值覆盖）；from_dict_error 5 案例冻结「Python 抛 ↔ C++ 抛」 | 46/46 消息形状覆盖；264 checks 0 failures | 通过 | R8 审核轮3 Karpathy |
| R8 | 审核轮3（Karpathy，subagent#2，2/3 配额）：2×P1+7×P2 全部采纳修复——P1 生成器确定性（随机 run_id/挂钟覆写，两次再生成字节级一致）、create_run 补确定性 smoke 测试（4 参重载+is_terminal+node() 命中/抛）；P2 白名单 map→set、RetryPolicy from_dict 去拷贝、include 卫生 5 处、json_to_bool 去空参、死注册项 data.search/x.y 删除、unknown-type 消息改 py_repr 并补非 string 冻结案例、minimum/maximum 污染即抛（Python TypeError 对齐）、D11.3 措辞更正（如实记录 required/enum 字符串为静默跳过） | 275 checks 0 failures | 通过 | R9 终验+PR |
| R9 | 终验：干净重建（rm build/conv-06 → configure → build 全树 116 target 无警告错误）→ ctest workflow_spec.validate Passed → 全树 ctest 两遍 29/29（含 mapping_kernel.* 8 项）；06-pr.md 落盘；commit 0cc79713（信息按 §9 原文）push origin；gh pr create #1322 | workflow_spec.validate 两遍绿；全树 29/29 ×2；PR https://github.com/WindWang2/paleo-workbench/pull/1322 | 通过（§8 全部满足） | 完成 |

## §8.9 开发量自我审计（2026-09-18 收口）

- **读过的文件数**：33 个全文（§5 清单全覆盖）——workflow/dag 4 + workflow 3 +
  contracts 6 + harness 3 + providers/execution.py + task_runtime.hpp +
  tests 8（§5 点名 3 个全读，另读 5 个 WorkflowSpec 关联测试）+
  scientific-workflow-v8 5 篇 + Pwb::Domain json/sha256 头 + engine.py 关键段 +
  根/mapping_kernel CMakeLists。
- **生成的 oracle 案例数**：150（valid 27 / invalid 46 / condition_trees 17 /
  slot_bindings 24 / resolve 18 / canonical_hash 9 / run_roundtrip 4 /
  from_dict_error 5）；C++ 侧断言 275 条全绿；错误消息形状覆盖 46/46。
- **审核轮次**：3（正确性=自查+subagent#1 对抗审核 16 发现/修 13 记 4；
  完整性=46 消息形状+全枚举覆盖审计；Karpathy=subagent#2 9 发现全部采纳）。
- **账本轮次**：CONV-06 区段 R1-R9 共 9 轮（≥ 实现 2 轮 + 审核 3 轮）。
- **subagent 配额**：2/3（对抗审核、Karpathy 审核），未超。
# CPP 转换 07 区段：内存 DAG 执行器（feat/cpp-conv-07-dag-engine，2026-09-18 启动）

Goal: 实现内存 DAG 执行：拓扑序、失败短路、取消、每节点 stdout 级日志；节点体用函数注册表（先只注册 no-op 与 mapping_kernel.interpolate_factor 包装）。Oracle 见任务书 §8。上限 200 轮；subagent 配额 3。

| 轮 | 改动 | 验证结果 | 判定 | 下一步 |
|---|---|---|---|---|
| 1 | 读强制文档（goal-loop/karpathy/main-plan/CLAUDE.md/domain.md/issue-tracker.md）+ 建 worktree（origin/main=35987e13，分支 feat/cpp-conv-07-dag-engine） | worktree 就绪、资源充足（49GiB 可用） | 通过 | 全文读 §5 Python 源 |
| 2 | 全文读 dag/{engine 1120,model 430,store 261,receipt 258,reproduction 67,validation 264,plan_view 203,__init__ 52}.py + runtime/{task_scheduler 707,cancellation 108}.py + harness/executor.py 466 | 状态机/短路/取消/绑定语义表完成 | 通过 | 读 C++ 侧与测试 |
| 3 | 全文读 task_runtime.cpp/.hpp、mapping_kernel interpolator（hpp133+cpp908）/extract（hpp65+cpp470）、interpolator_test.cpp 307、generate_interpolator_fixtures.py 400、test_workflow_dag.py 894、test_task_scheduler.py 268、scientific-workflow-v8 五篇、06-decisions.md（06 worktree 未跟踪文件；06 无代码） | API/异常/消息文案确认 | 通过 | 写三账本 |
| 4 | 写 07-findings.md（§5 逐文件逐符号）+ 07-decisions.md（D1-D11）+ 本区段 | 三账本落盘 | 通过 | 真实 Python 生成 oracle fixture |
第5轮 | 实现 libs/workflow_engine（engine.hpp/engine.cpp/ops.hpp/ops.cpp）+ 测试 engine_run_test.cpp + lib/tests CMake + 根 CMake BEGIN CONV-07 块 | configure OK | 通过 | 编译修错
第6轮 | 修编译：std::string==Json 三处改 get<std::string>；修 type_error.305：grid_x/grid_y 用一维 check_axis | 编译过，运行 abort 消除 | 通过 | 跑测试修语义
第7轮 | 修两处语义缺陷：(a) 驱动循环对已 SKIPPED 节点重复改写 reason（Python 只判 PENDING → 加 pending 守卫）；(b) CRS 应从 extract 输出经 $ref 链式流入 idw（Python dataset.crs 语义；fixture 改 dataset_crs 字段并重生成） | 直接运行 PASS 0 failures | 通过 | ctest ×2
第8轮 | ctest workflow_engine.run ×2 绿；全树 29/29 绿；mapping_kernel 8/8 ×2 绿 | 输出在最终回复 | 通过 | 审核 ×3
第9轮 | 审核轮 1（对抗性 spec 审核，subagent #1）：实跑 ctest+TSan/UBSan/ASan+探针。发现 0×P0、0×P1、5×P2：绑定失败下游 skip_reason 缺 ": <cause>" 尾巴 / 畸形绑定对象静默透传 / duplicate 抑制了非空环 / 非 std::exception 击穿引擎 / 测试 test.block 悬垂捕获 + time() 注释失真 + 未覆盖分支（ref_unresolved、search_radius、null→NaN） | 报告 PASS-with-notes | 通过（全部采纳修复） | 审核轮 2
第10轮 | 审核轮 2（Karpathy 质量审核，subagent #2）：发现 3×P1（ops.hpp 导出无人用函数且 "Shared with tests" 注释为假 / 公有头泄漏 mapping include 但链接 PRIVATE 的契约矛盾 / from_json 死代码零测试）+ 5×P2（description/name 死字段、validate_spec 可空指针无消费者、空 outputs≈unresolved 需注释、duplicate 决定只在代码注释里、D6 账本 7 字段与实现 4 元组不符、search_radius 漏列、未用 include） | 报告 NEEDS-FIX（仅表面卫生） | 通过（全部采纳修复） | 自审轮 3
第11轮 | 修复落地：bind-先行结构（skip_reason 尾巴对齐 Python 两分支）/ 畸形绑定 fail-closed / Kahn 恒跑（空 leftover 豁免、非空环照报）/ catch(...) 落 FAILED / 测试局部 registry / 删 description+name 死字段 / validate_spec 改引用 / ops 收敛到匿名命名空间 + 头不泄漏 mapping / from_json 接入测试 / 空 outputs 与 Python results-view 同构注释 / D2/D5/D6 账本修正。审核轮 3（自审完整性抽查）：逐函数核对覆盖——validate_spec(10 冻结清单+duplicate+引擎拒绝)、topological_order(每次 run+序断言)、Engine::run(成功/失败/绑定失败/三取消窗/预取消)、resolve_value(整包/带key/缺key/unresolved)、ops(noop/extract/idw+search_radius+null→NaN 直接调用)、CancelToken(三窗口)；补整包 $ref 成功路径案例 | 全树 29/29 绿；workflow_engine.run ×2 绿；mapping_kernel 8/8 ×2 绿 | 通过 | 提交+PR
第12轮 | commit（feat(workflow_engine): in-memory DAG engine with interpolate_factor node and failure short-circuit）+ push -u origin + gh pr create → PR #1320（正文含用户流程/oracle 案例数/本地 ctest 证据/「不依赖线上 CI」声明） | PR URL 回填最终回复 | 通过 | §8 全项核验后收口
## CONV-08 因子插值任务宿主 — 2026-09-18（worktree cpp-conv-08-factor-host，feat/cpp-conv-08-factor-host @ 35987e13）

目标:移植 interpolation_fingerprint / interpolation_evaluation 纯函数叶子与 interpolation_plan 数据结构进 libs/factor_host；不接 UI、不跑 geoviz 引擎。Oracle: ctest factor_host.fingerprint ×2 绿 + mapping_kernel 全绿 ×2 + PR 开出。上限 200 轮。subagent ≤3。

| 轮 | 改动 | 验证结果 | 判定 | 下一步 |
|---|---|---|---|---|
| R1 | 读 goal-loop/Karpathy/主计划/CLAUDE/agents 约定；建 worktree | 分支 feat/cpp-conv-08-factor-host @ 35987e13 核验 | 通过 | 读源文件 |
| R2 | 全文读 fingerprint(667)/evaluation(825)/plan(676) | 纯叶子清单成形 | 通过 | 读余下源码 |
| R3 | 全文读 factor_interpolation(1488)/factor_prepare_scheduler(769)/sample_normalization(231) | 编排层无数值叶子；normalize 已有 C++ | 通过 | 读测试 |
| R4 | PySide6 缺失→importlib 按文件加载真实 geoviz 叶子+sys.modules 注入，验证 import 真实 workflow 模块 | sha256/xy_sig/resolve_backend 真实输出 | 通过 | 读测试 |
| R5 | 全文读 13 个相关 pytest（353/376/269/160/412/659/155/1001/119/1353/169/590/210 行） | 断言→oracle 案例表；7 项测试缺口 | 通过 | 读文档 |
| R6 | 读 v9 因子产品章 02/06/08；确认复用 pwb::domain::Sha256 | 约束入 findings | 通过 | 写档 |
| R7 | 写 08-findings.md（符号全索引）+ 08-decisions.md（D-1..D-14） | 14 项决策落档 | 通过 | oracle 生成器 |
| R8 | 写 oracle 生成器 tools/oracle/generate_factor_host_fixtures.py（importlib 真叶子 bootstrap；geo-viz-engine 回退 main 工作区路径）| 162 案例冻结 232KB；classify 13 态矩阵修正（组件隔离构造）后全对 | 通过 | C++ 实现 |
| R9 | canonical_json（python repr double + json.dumps sort_keys/compact/ensure_ascii=False 逐字节复刻）+ fingerprint.cpp（extract/build/classify/stored/has_output 全叶子）| 编译过 | 通过 | evaluation/plan/interop |
| R10 | evaluation.cpp（bilinear/signed_r2/metrics/folds/lowo/cross_validate/surface_residuals/features/context/adjudicate）+ plan.cpp（xy_sig/fault_sig/PlanKey/linspace/build/aligned/PlanCache/round-trip）+ interop.cpp（engine method/variogram/task params）+ 根 CMake BEGIN CONV-08 | 编译过 | 通过 | 测试 |
| R11 | factor_host_tests/fingerprint_test.cpp（消费全部 162 案例 + artifact 文件分支自测 + PlanCache LRU 自测）| 首跑暴露 6 处（restore_sentinels 未调/plan_key 缺省键/2D grid_z/to_dict 无 residuals/null-metrics 无 recommended/mismatch 需参照 plan）逐个修 | 通过→修复→通过 | 对账 |
| R12 | 对账绿：./factor_host.fingerprint = PASS 3166 checks 0 failures；生成器重跑 sha256 逐位一致（可复现）| ctest factor_host.fingerprint ×2 绿；全树 29/29 ×2 绿（含 mapping_kernel 8 件 + data 20 件）| 通过 | 三轮审核 |
| R13 | 第一轮审核（对抗性正确性 ×1 + Karpathy ×1，两 subagent）：82 组两侧对比 + 全量清单 | 发现 1×P1（bilinear 退化网格越界读）+6×P2（grid_z is-not-None/id 回退/-0.0 归一泄漏/字符串 float 超集/method 数字崩溃/Unicode strip）+14×P3 | 未通过 | 修复 |
| R14 | 修复全部 P1/P2：带符号负索引复刻 numpy wrap、is-not-None 与真值两语义并存、raw_float 与 finite_double 分流、严格 Python float 文法、python_str_scalar、Unicode python_strip（内部 semantics.hpp 单源）、通用异常兜底落地 D-6、NaN 角比较器、repr 引号、P3 死代码清理 | fixture 增至 179 案例（每个修复分支都有冻结案例）；PASS 3370 checks 0 failures | 通过 | 全量复验 |
| R15 | 第二轮审核（复验，续用对抗性审核 agent）：9 项修复 7 项完全确认；3 尾巴（nan(1) 词匹配早退 / float-int 路径 \x1c-\x1f 过度剥离（新发现：Python 数值解析不接受文件分隔符，与 str.strip 不同集）/ resolve_engine_method 引号漏改）| 184 组对比实验；fixture 可复现性逐字节确认 | 未通过→修复 | 尾巴修复 |
| R16 | 尾巴修复：is_python_float_literal 补 i==n 尾检、semantics 拆 python_strip_number（float/int 专用集，不含 \x1c-\x1f）、interop 错误文案换 python_repr_string；fixture 增 nan_payload/filesep/引号三案例 | 182 案例 3397 checks 全绿；全树 29/29 ×2；factor_host.fingerprint ×2 | 通过 | 提交 + PR |
# CONV-09 DTW kernel — goal-loop ledger
第1轮 | 建 worktree cpp-conv-09-dtw（BASE=35987e13）；读 goal-loop/karpathy/cpp-conversion-main-plan/CLAUDE/domain/issue-tracker | 分支 feat/cpp-conv-09-dtw 就绪，BASE 一致 | 通过 | 通读 §5 源文件
第2轮 | 全文阅读 §5 源码：dtw_log_matcher.py（5 公开符号）、stratigraphic_correlation_engine.py、formation_top_correlator.py、well_log_api/load/track_layout、welllog_engine_adapter、welllog_multi_well_adapter、workflow/stratigraphy_correlation+correlation_artifact/lifecycle/overlay/session、ui/pages/dtw_propagation_worker | 确认数值核仅 dtw_log_matcher.py；其余为消费者/胶水；backtracker 用 == 精确比较破平局 → 需位级对账 | 通过 | 读测试与文档
第3轮 | 全文阅读 10 个测试文件（test_dtw_log_matcher、test_issue1054、test_dtw_propagation_worker、test_correlation_engine_depth_step、test_correlation_link_editing、test_formation_top_correlator、test_stratigraphic_correlation_engine、test_stratigraphy_correlation{,_tops,_ui}）+ ADR 0003 + v6 00/01/02 章 | 断言翻译为 oracle 案例表（shift/NaN/不等长/常数/降采样/窗口拒绝/平局量化曲线） | 通过 | 验证 oracle 解释器与 numpy 求和可复刻性
第4轮 | 确认 oracle 解释器 = main/.venv/bin/python（3.12.13, numpy 2.5.2，可 import 真实模块）；在 1500+ 随机数组（n=0..65537）上验证标量 pairwise-sum 算法对 np.mean/np.std 位级全等（0 失配） | C++ 复刻同算法 ⇒ 归一化曲线位级一致 ⇒ 路径索引应全等 | 通过 | 写 findings/decisions，开始实现
第5轮 | 实现 libs/well_science（dtw.hpp/dtw.cpp：numpy pairwise 求和复刻 + normalized/min_max_downsample/match_curves/transfer_top_index）+ well_science.dtw 测试 + BEGIN CONV-09 CMake 块 + tools/oracle/generate_dtw_fixtures.py | 生成器（venv python，真实模块）冻结 53 案例（10 norm + 9 ds + 27 match + 7 transfer）；首轮测试抓到 bin_argext 绝对/相对索引错位 bug | 修复 | 重建重跑
第6轮（审核1·正确性）| 逐行对照 dtw.cpp ↔ dtw_log_matcher.py：归一化顺序（finite 均值插补→std 门→插补后均值除）、降采样恒等/末 bin/序序规则、match 流程序（降采样后再判窗）、回溯 elif 链（斜>上>左）、transfer 严格小于+zip 短截断 | 修复索引错位后 53/53 PASS：cost 位级相等、路径逐元素全等、norm_mean_overflow 的 -inf 输出 null 约定成立 | 通过 | 完整性审核
第7轮（审核2·完整性）| 分支→案例映射核对：normalized（空/NaN/±inf/全NaN/单点/常数/符号溢出/均值溢出/大偏移）9 分支 10 案例；downsample（n=0/bin≤1/min<max/max<min/持平/末尾残 bin/并列首下标/NaN）8 分支 9 案例；match（空×2/stride1/stride8/stride40/窗拒绝/窗边界/窗0/对角/左缘/底缘/量化平局/溢出 inf 代价/全 NaN/float32 上转）27 案例；transfer（空×2/并列/就近/越界/zip）7 案例 | 每个公开函数的每个失败/边界分支至少 1 案例覆盖，无缺口 | 通过 | Karpathy 审核
第8轮（审核3·Karpathy）| 检查新增面：4 自由函数+1 结构体+1 常量，无类壳/无配置旋钮/无模板/无投机扩展；-Wall -Wextra -Wpedantic 零警告；头文件冗余 include 清理；identity indices 与 Python arange 语义一致保留；CMake 仅追加 CONV-09 块 | 无多余抽象，每行可追溯到移植或验收需求 | 通过 | ctest ×2 + subagent 对抗审核
第9轮（subagent 对抗审核 + 质量审核）| 配额内 2 个 subagent 并行：①对抗 spec 审核（逐行对 Python，独立重导 53 案例对账、独立验证 pairwise 算法位级等价）= PASS 无 P0/P1，3 条 P2；②Karpathy 质量/边界审核（git diff 变更面、submodule 未动、-Wall 零警告、生成器逐字节复现）= PASS 无 P0/P1，3 条 P2 | 采纳 4 条低成本修复：fixture 非有限值 null→"inf"/"-inf"/"nan" 字符串标签（消除 NaN/±inf 误判面）；transfer min_dist 改 Python 同款 double inf 哨兵；局部 std 改名 std_dev；文档 D11/findings 乘法描述改准（int64）。其余 P2（int64 溢出域≈3e9 采样、llabs(INT64_MIN)）属现实不可达域，按最小码原则不加投机防御，已在 decisions 备案 | 修复后 53/53 PASS；全套 29/29 ×2 + dtw 单项 ×2 + mapping_kernel 8/8 ×2 | 通过 | commit + PR
