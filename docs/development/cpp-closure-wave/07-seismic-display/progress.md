# 07 — progress

| 轮 | 候选 SHA | 目标/范围 | 命令/退出码 | 结果 | 下一步 |
|---|---|---|---|---|---|
| 0 | 06211541 (origin/main) | 盘点：平台 /goal 支持、seismic 全链现状、04/12/06 协调合同、Python 冻结源语义、LOD/GPU/Zarr 裁决复查 | git fetch（origin/main=06211541，开放 PR 为空）；Explore 子代理盘点（~3.97M tokens，1/3）；读 seismic_viewer/ui_wellseis/viz_e_install/volume_service/algorithm.hpp/attributes.py/seismic_view.py | 完成：缺口 5 项记录于 task_plan/findings；无 /goal 技能如实记录；SDK cmake 路径确认 | R1 Qt-free 核实现 |
| 1 | 06211541+R1-R3 | R1 Qt-free 核（attribute_fusion_core / slice_export / view_state）；R2 widget 集成（retained_plane/set_attribute_plane/set_rgb_fusion/export_slice/save_view_state/load_view_state + 切片变更钉扎失效）+ viz_d_seismic_install 导出/视图态菜单；R3 closure_seismic_install（show_resource→SeismicVolumeService open→RealSeismicViewBinding→set_view；属性面板→E 线核→属性视图；RGB融合=最近三平面；结构核诚实拒绝）+ app_shell/main_window 具名块 + ui_wellseis 词表 RGB 叶子 + CMake 块；新增测试 3 套件注册 | 资源门 Probe 一次成功（38.5GiB 空闲）；Configure 三次失败后定位两个原因：(1) 根 CMake UI-14 imply 链置 CONV_07=ON 但未置 MAPPING_KERNEL/CONV_06（现主线缺陷，非本线所有，已用 -D 显式补参绕开，不抢改 12 的文件）；(2) gate 脚本 -a 参数需自带 -D 前缀；一次 RESOURCE_BUSY（holder 521729）退避重试中 | 代码全部就绪；门控 configure 后台进行 | 构建修复编译错误 → 测试 |
| 2 | 同上+修复轮 | 门控构建与测试修复循环：修 closure hpp 不完全类型 QObject/QString（改真包含）；测试宏内逗号拆参（改为逐成员比较）；put_be_f32 转换；advanced_core 补 sv_test_main.inc；closure 测试补 QApplication main（viz_d.widget 先例）；测试 SEG-Y 迹头偏移 188/192（0-based）；science 注册 id 需 "seismic." 前缀；RealSeismicViewBinding 悬垂 widget 用 QPointer 修（Qt 父子删除顺序下原 raw 指针双删除）；npy 头 64B 对齐差一——修复循环共 5 轮门控 Build+Test，全部 RESOURCE_BUSY 退避遵守 | 门控 Build/Test 多轮 rc=0；viz_d.closure 4/4、seismic_viewer.advanced_core/advanced_widget PASS | 新套件全绿 | 全量受影响回归 ×2 |
| 3 | 同上+审查修复 | 独立审查子代理（general-purpose，~2.44M tokens，2/3 subagents）静态审查全部 diff+Python oracle 对照：P0=0；P1×3（RGB融合通道可跨体/跨片混拼、属性钉扎下 npy/csv 导出振幅面与显示不一致、viz_d.closure CMake 前向引用在特定配置炸 configure）；P2×9。修复：P1-1 属性历史带 (volume,axis,index,revision) 身份+配准校验+换体清空；P1-2 属性激活时 npy/csv 诚实拒绝（PNG 仍导真显示），hpp 契约注释同步；P1-3 门条件改为 PLATFORM+DATA（UI imply 触发条件，保证 UiWellseisQt generate 期存在）+补 RetainedPlane.index；P2 修复：size_t 乘积、csv nan 无符号、内嵌 picks 体绑定校验、换资源回放 display_mode、main_window 注释对齐、篡改注释诚实化。P2 遗留（记录 findings）：wiggle 下钉扎属性不可见但仍激活、load_view_state 两个 no-op 语义点、属性失败诊断不上屏 | 门控 Build（全量 701 目标）rc=0 零失败；ctest 受影响集 37/37；复验第二遍进行中 | 审查修复完成 | 复验 → 提交 → PR |
