
## CONV-31 catalog 域核（feat/cpp-catalog）
- 轮1 侦察：31-findings.md 三列 scope（35 文件逐一点名终态）+ 31-gap-analysis-big.md（8 巨文件行号级差距）。
- 轮2-4 实现：15 个新 TU 并入 pwb_catalog（CONV-31 CMake 块）。
- 轮5-6 oracle：generate_catalog_domain_fixtures.py 冻结 37 节；data.catalog_domain replay 13 用例全绿×2 + negative 5 处篡改全检出（g++ 直连，无 cmake 如实披露）。
- 轮7-8 接线：本切片为库层（不触 AppContext；宿主接线随消费方切片）。
- 轮9-11 review：自查修复 = DataError Ok 哨兵×3、RunPort dump 键集（无 direction）、oracle 走文档扫描路径（batch_depth 门）、宏双重求值假阳性。
- 轮12 台账+PR：31-decisions.md（D1-D6，含 31b 拆分）。

## CONV-31b catalog service/db 深核（feat/cpp-catalog-service）
- 轮1 侦察：10 并行代理 R1-R10（31b-recon/；契约提案+行号锚点；R10 实测 g++ 直连全链路 13 用例×2）。
- 契约冻结：31b-findings.md（A1-A8 所有权地图+§C 共享契约+§D 16 裁决+§E 34 偏离）+ 10 新头签名桩（聚合 TU 零警告）+ models.hpp Model/ModelVersion DTO 实装。
- 轮2-4 实现：8 并行代理 A1-A8 零交集交付（apply_changes/queries_sql/service_core/working_copy/resolve+trash_service/model_registry 三 TU/v11_bundle+place_managed_tree/repository 扩展）；并行冻结 oracle 97 节（两遍逐字节一致）。
- 轮5-6 oracle：data.catalog_service replay 25+3 cases ×2 全绿（95/97 节全值）+ negative 8/8 检出；CMake 两处 CONV-31B 块；replay 顺带揪出 4 真实缺陷（含 2 UAF）修复。
- 轮7-8 接线：库层切片（不触 AppContext；宿主接线随消费方切片）。
- 轮9-11 review：V1-V4 四镜头并行审查（parity/内存并发/约定/消费者；P0×1 ASan 实锤 UAF+P1×8）+ 集中修复 8 项；ASan/UBSan 全闭包零报告；28 项测试 ×2 零回归（含 Python readback 门）。
- 轮12 台账+PR：31b-decisions.md（D1-D7；D4 全缺陷账、D5 有界偏离含 Transaction::commit 吞 rc 遗留项、D7 CONV-33 catalog 依赖就绪）。
