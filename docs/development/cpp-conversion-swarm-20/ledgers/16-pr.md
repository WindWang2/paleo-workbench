# 16 — UI 面板承接清单 + 第一簇只读因子统计 HUD（conv-16）

分支 `feat/cpp-conv-16-ui-panel-cluster`（BASE = origin/main `35987e13`）。

## 用户流程验收

**跑完属性或插值后，C++ dock 显示 min/max/mean/valid_count，与核统计一致。**

- 新 `FactorStatsDock`（`apps/paleo_workbench_platform/factor_stats_dock.{hpp,.cpp}`）：
  只读 QDockWidget（objectName `factor-stats-dock`，标题「单因素统计」），五行值卡
  **因素 / 取值范围 / 均值 / 标准差 / 有效格元**，输入即 `pwb::mapping::GridStatistics`
  （= Python `FactorGrid.statistics`，M7 已对冻 oracle）。渲染词表复刻 Python 工作站
  `inspector.show_factor` 的 grid 行契约（`f"{min:g} ~ {max:g}"`，
  `tests/test_inspector_v7.py` 钉死 "10 ~ 220.5"）；非有限值与全 nodata 显示 "—"，
  与 Python 的 dash 回退逐字一致。
- MainWindow 接线（`main_window.{hpp,cpp}` 最小守卫钩子，全部 `#ifdef PWB_WITH_CONV_16`）：
  `buildUi` 挂到右 dock 区 + `showFactorStatistics(name, stats)` 入口（插值流落地的调用点）。
  关闭 CONV_16 后与 BASE 逐字节等价（ Karpathy 审核静态核实）。
- 测试 `platform.factor_hud`：11 个冻结 Python oracle 案例逐字段对账——`grid_statistics`
  数值（1e-12 相对容差）×5 行渲染文本（逐字，含 `:g` 指数格式 `0.0001 ~ 1.23457e+06`、
  float32 舍入、全 NaN、空网格）+ dock 可挂 QMainWindow + 空 factor 名回退。

## 清单（本任务主交付之一）

`docs/development/cpp-ui-panel-inventory.md`：`paleo_workbench/ui/pages/` **全部 132 个 .py**
逐文件裁定（用户任务/策略/C++ 落点/依赖核现状）：

| 策略 | 文件数 | 说明 |
|---|---|---|
| QGS 原生 QGIS 承接 | 13 | QgsMapCanvas/QgsLayerTreeView/QgsMapTool/QgsVectorLayer 家族 |
| QT 新 Qt widget | 76 | QtWidgets 直译（含与本 dock 同构的「值卡族」面板） |
| CORE Qt-free 核+薄接线 | 33 | 可对冻结 Python oracle 的纯逻辑（oracle 富矿） |
| DEFER 延后 | 10 | 依赖 M8 viz/geoviz 引擎或 M9 服务层，语义先行不伪造 |
| RET 退役 | 0 | legacy 面按「Qgs 就绪后降级为兼容回退」处理，不删除 |

逐文件阅读笔记：`16-findings.md`（父代理 132/132 节）+ `16-agent-findings.md`
（subagent 68 节：workstation 30/30、qgis_stack 8/8、ui 顶层 5/5、C++ app/libs、32 篇 UX/cpp-platform 文档）。
每个非显然选择见 `16-decisions.md`（D1-D8）。

## 方法（与已落地 mapping_kernel 一致）

oracle 生成器 `tools/oracle/generate_factor_hud_fixtures.py` 用**真实 Python 模块**
（`paleo_workbench.workflow.factor_grid_result` 的 `GridStatistics.from_grid` 与
`FactorGridResult` 生产路径）冻结 11 案例；期望值（数值+显示字符串）全部由真实模块与真实
f-string 产出，零手写。审核 subagent 独立在内存重跑 11 案例，与冻结 fixture 全字段相等。

## 变更面（外科手术边界）

- 新增：`apps/paleo_workbench_platform/factor_stats_dock.{hpp,cpp}`；
  `tests/cpp/platform/factor_hud_test.cpp` + 冻结 fixture
  `tests/cpp/platform/fixtures/factor_hud/oracle.json`；
  `tools/oracle/generate_factor_hud_fixtures.py`；
  `docs/development/cpp-ui-panel-inventory.md`；ledgers `16-{findings,agent-findings,decisions,pr}.md`。
- 修改：`main_window.{hpp,cpp}` 仅 CONV-16 守卫钩子（hpp 前置声明+成员+1 方法；cpp
  include+挂 dock 3 行+转发 8 行）；根 CMakeLists 与 apps CMakeLists 各一个
  `BEGIN/END CONV-16` 注释块**纯追加**（其他 CONV-* 块零触碰，git diff 核实）。
- libs/ui 本切片**零改动**（无需要泛化的复用面；dock 单点落地，勿过早抽象）。
- 诚实边界：本机无 QGIS vendor 树，验证树为 PLATFORM=OFF 配置——「dock+oracle」全链实测；
  「MainWindow 内 dock 接线」在该配置不可链接（Pwb::Application 不参与），CONV_16+平台
  同开的编译正确性已由审核实证修复（GridStatistics 前置声明）并静态核验，但仍如实声明
  未在本机做过该组合的完整链接。自动「插值完成→dock 刷新」待 mapping 管线进 app（后续切片），
  本切片交付 dock + 显式入口。

## Oracle 案例数（11 冻结案例 × 每案例 10+ 断言）

seq_2x2 / with_nan / all_nan / with_inf / single_cell / empty_grid / constant_field /
negatives / float32_rounding / large_magnitude_g / via_factor_grid_result（生产路径）。

## 本地 ctest 证据（全部本机真实运行，**不依赖线上 CI**；未等待任何 GitHub Checks）

```text
构建：cmake -S . -B build/conv-16 -G Ninja -DCMAKE_BUILD_TYPE=Release \
  -DPWB_BUILD_PLATFORM=OFF -DPWB_BUILD_DATA=ON -DPWB_BUILD_SCIENCE=OFF \
  -DPWB_BUILD_MAPPING_KERNEL=ON -DPWB_BUILD_CONV_16=ON -DBUILD_TESTING=ON

ctest -R 'platform.factor_hud'  ×2 → 100% tests passed, 0 tests failed out of 1（两遍）
ctest -R 'mapping_kernel'       ×2 → 100% tests passed, 0 tests failed out of 8（两遍）
ctest -R 'data'                 ×2 → 100% tests passed, 0 tests failed out of 20（两遍）
ctest（全量）                   ×2 → 100% tests passed, 0 tests failed out of 29（两遍）
```

## 审核

三轮：R2 对抗性 spec 审核（subagent #2：独立重跑真实 Python 重建 11 案例全等、
Qt 'g' vs Python `:g` 31/31 逐字探针、守卫 diff/CMake/证据核对——发现 1×P1
[GridStatistics 无声明来源] + 4×P3，全修复）；R3 Karpathy/质量审核（subagent #3：
84/84 行纯插入全可追溯、关 CONV_16 逐字节回 BASE、-Wall/-Wextra/-Wpedantic 零告警——
结论「可放行」，1×P2 [namespace 内 include→前置声明] + P3 已修）；父代理清单漏文件专项
（inventory 132/132、findings 132/132、agent-findings 全组覆盖，程序化校验零缺失）。
subagent 配额 3/3 用满。
