# CONV-25 — dependency_graph + evidence 选择器/解析核移植

## 范围

`paleo_workbench/workflow/dependency_graph.py`(391 行,全量)+ `interpretation/evidence.py`(464 行,全量)→ `libs/workflow_graph`(`pwb_workflow_graph`,Qt/Python/numpy-free,仅依赖 `Pwb::Domain` 有序 JSON)。

移植:

- **dependency_graph**:DataVersionRef/DataRunRef 精简镜像(仅图行为所需字段)、`rebuild`(全索引:producing_run/consumers/run_inputs/run_outputs/version_asset/asset_versions/domain_task_runs/edges)、三色 DFS `cycle_nodes`(只标回边端点——忠实 Python 怪癖)、`asset_id_for`、`direct_/transitive_downstream_runs`(BFS + max_nodes 步数封顶)、`transitive_downstream_versions`、`latest_run_for_domain_task`(取最后追加)、`find_reuse_run`(status 过滤 + 有序输入比较 + 参数子集 + 反向插入序)、`topological_runs`(版本边 + map_compile/task_consumers 合成边 + 可达性守卫 + Kahn 右 append)。
- **evidence**:EvidenceKind/Status 枚举、`format_evidence_selector`(含 `current` 撞名拒绝)、`parse_evidence_selector`(五前缀 + `constraints:current` + `_looks_like_version_id` 兜底 + repr 文案)、`EvidenceResolution`(is_usable/to_dict)、`resolve_evidence` 五路分派、`available_evidence`。

排除:`constraint_versions.py` 本体(content-hash 子系统 + catalog models + pydantic——超界),按 seam 注入 verdict 回调。

## 关键设计

- 四处 seam 全部注入化:`CatalogResolver`(nullopt=无目录 vs 查找失败,两态语义不同)、`WorkspaceView`(membership/layers_with_role)、`ConstraintResolver`(document,ref)→{status,detail}、document 走 JSON 视图。
- 插入序字典语义:producing_run/consumers 等用 vector+index 保 Python dict 序(冻结输出逐键对账)。
- Python 细节忠实:`setdefault` 首者胜、`list(input_ids)` 有序比较、`max((started_at,rid))` 选 latest、`sorted()` 出队 + 右 append Kahn、`str(x or "")` 强转门禁、UTF-8 码点 len()、`{text!r}` repr 消息。
- oracle 的 constraint 路径全部注入 verdict(含 avail 语境的 clean/raise 两支)——不冻结真实子系统的环境相关 AttributeError。

## Oracle 覆盖(131 案)

graph_rebuild 8(空/孤儿/自环/双环/混合环/重边/版本先行)、direct 3、transitive_runs 7(含 capped/cycle)、transitive_versions 2、latest_run 3、asset_id_for 2、find_reuse_run 13(op/status/有序输入/gv/hash/params 子集/require_outputs/反向最新优先)、topological_runs 9(链/菱形/子集环/集外环/map_compile 合成边/task_consumers)、format 14(五 kind + current 撞名 + ref_id=123 强转)、parse 27(空前缀/多冒号/裸 ver_/dver_/uuid/sha:/非字符串)、resolve 38(draft 7 分支/factor 含 stale+quality+mock/prediction 含 status 门槛+unpinned/constraints float+named 全 status_map+raise/catalog_version 三态/parse 冒泡)、resolve_selector_obj 1、available_evidence 4。

## 验证

- `workflow_graph.contracts`:131/131 通过,0 failures
- 全量 ctest 22/22(data_suite 21 + workflow_graph.contracts)
- 生成器重跑 byte-for-byte 一致(sha256 e496e1b1…)
- CMake:`PWB_BUILD_CONV_25` 默认 OFF,imply `PWB_BUILD_DATA`;块声明在 data_suite 子目录之前(CONV-23 同例)
