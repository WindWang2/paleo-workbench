# VIZ-D goal-loop ledger (seismic advanced 2D display & presentation delta)

- Branch: codex/viz-d-seismic-display | base origin/main f0af9d4e45d9086093f88022d0e4819295cd9434
- Worktree: /home/kevin/projects/paleo_project/worktrees/viz-d-seismic-display
- Oracle freeze source: geo-viz-engine submodule @08851951f3bbc0beb90886adf52e1928f4383c16

## Rounds

- R1 | 定位仓库、fetch origin、建 worktree viz-d-seismic-display（f0af9d4e）、init geo-viz-engine submodule、读 completion plan V5 + PR #1369/#1368/#1375/#1376/#1394（全 MERGED，#1380/#1381 P1 store 并发仍 OPEN） | worktree 就绪 | 通过 | R6a/R6/R8 审计
- R2 | 四路并行审计完成（R6a 基础件/VD-wiggle 语义/horizon-crossplot-colormap-stratal/PreviewKind），审计结论落盘 docs/development/cpp-viz-d/r6a-audit.md；纳入缺口 A1 任意线 CPU parity、A2 prefetch、A3 预览异步 presenter | 审计文档就绪 | 通过 | 实现核心显示库
- R3 | 实现 seismic_viewer 新核：display_core（percentile_clip_range/normalize_to_index NaN→128/viewport_decimation/wiggle_geometry/sample_polyline_slice）、color_maps 扩展（seismic 5-stop parity + seismic_r/gray/jet/hsv/viridis/phase_wheel，保留 grayscale/heat）、horizon_core（quad_faces/parse/fill_nearest EDT/fill_rbf/extract_along_horizon/拾取模型+JSON+CSV）、isosurface_core（marching tetrahedra 移植）、crossplot_core（GR/AI 统计 + envelope/inst-freq 经 science SDK 复用）、colorbar_widget、SeismicSliceWidget 集成（模式/极性/percentile/增益/拾取 UI）| 门禁 Build pwb_seismic_viewer 通过（0 error 0 warning，jobs=2，build/viz-d，QGIS deps 覆盖 = main/native/gdal-vendored/install 同 viz-c 先例）| 通过 | SliceController prefetch + 预览 presenter + 绑定适配器
- R4 | oracle 冻结（6 个 fixture，全部真实 Python/native 运行）：display（colormap/percentile/normalize/decimation/wiggle 真实 paintEvent 捕获/polyline）、horizon、crossplot、wellhead、isosurface（本 worktree 以冻结源重建 native oracle 模块于 build/viz-d） | 生成器 5 个落盘 tools/oracle/generate_viz_d_*.py | 通过 | C++ 对账测试
- R5 | tests/cpp/viz_d 三套件（core 12/widget 5/preview 2）全部通过。对账修复：linspace 舍入（i*(1/(n-1))）、numpy _lerp 中点分支 + float32 插值、polyline endpoint/float32 距离、fill_nearest 重写为精确整数距离菱形搜索（scipy 平局规则 min(|Δrow|,row,col)）、fill_rbf scipy linear 核 φ(r)=-r + 邻居集全含 fixture、MiniJson skip_ws、crossplot 核输入转置（sample-major→trace-major）| 三套件 19/19 通过 | 通过 | 全量构建 + ctest 两遍
- R6 | 全量构建通过（806 目标）。ctest 第一遍 126 测试 124 过：seismic_viewer.contracts 失败（注册表扩为 9 名超集，冻结断言需随 V5 差集更新——D 管辖测试）；ui_pages_preview.qt_widgets_smoke 失败为预存主机环境（GLX/libodbc，#1394 已记录，二进制直跑 ALL PASS） | 记录于账本 | 部分通过 | 修复审核发现
- R7 | 三轮独立审核回收（科学语义 P0:2 P1:3；生命周期 P1:2 P2:6；接线/构建 P0:3 P1:3 P2:6）。已修复：nlohmann 路径、app 条件编译 viz_d 源、viewer→attributes 根级蕴含 + ui_pages_preview 回退补属性库、polyline float32 坐标量化、normalize float32 链、常量面纯零/全 NaN 面中心区分（NaN 范围不缓存——有意优于 Python 的兄弟面毒化，声明）、MiniJson 深度上限 64、u64/i64 有限范围守卫、dt_ms==0 守卫、scan_number "1.e3"/尾点语义+循环化、save_picks 绑定不覆盖为空、零轴 clamp 守卫、contracts 测试超集断言、performance/main_window 标记包裹、杂散空行 | 等锁重构建 | 进行中 | 重构建 + ctest 两遍
- R8 | 审核修复后全量构建 + ctest 两遍：126 测试 125 过（唯一失败 ui_pages_preview.qt_widgets_smoke 为预存 GLX/libodbc 主机环境问题，#1394 已记录；二进制直跑 ALL PASS）。MALLOC_CHECK_=3 审计 viz_d+seismic_viewer+platform.attribute 9/9。OFF 配置（PLATFORM=OFF）configure+build+viz_d 2/2（preview 诚实缺席）。独立入口 cmake -S libs/seismic_viewer 配置通过 | 全绿 | 通过 | 补 fixture 覆盖 + 提交
- R9 | 审核驱动的 fixture 扩展：horizon parse 增 exponent_and_trailing_dot（"1.e3"→1、"100.5."→100.5、指数正常）；polyline 增 large_coords（64×48×16 体、2000+ 坐标，float32 量化敏感）。随之修复 scan_number 两处真实缺陷（裸点死循环、指数错误粘连——"1.e3" 曾被解析为 1000） | viz_d.core ALL 12 | 通过 | 提交/PR
