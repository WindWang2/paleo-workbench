# 10 — Topology M0 (V11)

## 1. 范围

M0 = 地基（CRS 契约 + 存储通道的 Python 侧）。M1–M5（原生顶点工具 /
跨层 / 几何命令 / 检查器 / 退休）待原生编辑会话落地——本 goal 内 M0
完整，M1+ 按规格 §8 逐里程碑领取（入口 = docs/specs/ 规格）。

## 2. CRS 契约（#1285，crs_gate + 控制器接线）

* 进前域校验：声明地理域 vs 数据实际范围（已知事故形态 4326+本地坐标）；
  失配阻止进入编辑 + 引导修复（declare_local/clear → 清空层声明，修复即放行）。
* 同一检测在工程打开时可扫全层（collect_crs_mismatches，一次修复永久生效）。
* 会话内 CRS 冻结（apply_project_crs 会话期拒绝改声明）。
* 层级声明为准（工程级声明是地图上下文，不为层级失配背书）。

## 3. 存储通道（#1283，Python 侧）

* save-intent 缓冲：未提交会话对发布不可见；提交/回滚推进基线。
* 全或无保存：两阶段 flush（门禁不过全体保持打开；提交中途失败剩余回滚）。
* 样式允许/字段拒绝、提交后对齐、台账冻结——待原生会话（M1+）。

## 4. 验收映射（规格 §7 S1–S4 落地情况）

S1（RAW 拒绝）——既有门禁（未动）。S2（CRS 引导）——本 M0（测试钉死）。
S3（全或无保存）——本 M0 两阶段 flush（测试钉死）。S4（崩溃易失）——
既有语义（未提交不落盘；rollBack 基线），未新增 sidecar（按 #1281 决议）。
