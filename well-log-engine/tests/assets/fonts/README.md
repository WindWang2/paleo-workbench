NotoSans-Regular.ttf is bundled as a fixed test font so text pipeline
tests are deterministic. It ships with the noto-fonts distribution
license (see NOTO-LICENSE.txt, copied from the upstream package).
