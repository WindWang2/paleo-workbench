# 高性能开放式测井绘图软件包与独立桌面应用技术方案

> **文档角色**：北极星愿景与目标架构（非逐文件 as-built）。  
> **进度与执行**：以 **§15 实施计划**（现状快照、已决策偏离、轨 F/D/E）为准；引擎实现细节见 `well-log-engine/docs/` 与 `docs/adr/0004+`。  
> **2026-08 对齐共识**：分层真源；单井+连井功能并行；`well_log_workstation` 升格为独立 WellPlot Desktop 发行物；导出 B0/B1 分期；插件 Runtime 后置。

## 1. 项目概述

### 1.1 建设背景

测井绘图软件是油气勘探开发、地层对比、储层评价、油气水识别及地质综合研究的重要基础工具。其核心技术难点包括超长深度区间下的高频曲线绘制、多图道同步平移与缩放、复杂岩性花纹与解释成果实时显示、图形与数据双向编辑、严格物理比例尺控制，以及长卷图件的高精度矢量导出。

本项目拟建设一套面向 Windows 和 Linux 双平台、具备自主可控能力的高性能开放式测井绘图产品体系。产品体系由“可独立集成的软件包”和“面向最终用户的独立桌面应用”两部分组成。

测井绘图软件包提供数据管理、工程模型、图形渲染、图道组件、交互编辑、模板、打印导出、插件运行时以及 C++/Python SDK，可被其他地质研究平台、解释软件或行业应用嵌入调用。独立桌面应用基于该软件包构建，作为可直接安装运行的完整产品，统一支持单井绘图、单井解释和连井地层对比，并提供项目管理、工作区、菜单工具栏、属性面板、插件管理和成果输出等完整桌面功能。

平台在产品功能和交互模式上对标 ResFormSTAR、ResForm GeoOffice 等成熟测井解释与绘图软件，同时采用开放式微内核架构，将数据解析、图道绘制、专业解释、图形交互、模板管理、算法分析和成果导出等能力设计为可插拔模块，为行业用户、科研团队和软件开发单位提供稳定的二次开发能力。

### 1.2 建设目标

平台建设目标包括：

1. 建设可独立发布和嵌入的软件包，提供稳定的 C++ API、Python API、Qt 控件和插件扩展接口。
2. 在软件包基础上建设一个独立桌面应用，作为面向地质专家和测井工程师的完整产品。
3. 桌面应用统一支持单井工程和连井工程，提供单井绘图工作区、连井对比工作区及其成果联动。
4. 构建基于 C++20、Qt 6、OpenGL 3.3 Core 和 Apache Arrow 的高性能测井数据与图形内核。
5. 支持 LAS、DLIS、LIS、716、ASCII、Excel 等常见测井数据格式，并允许通过插件扩展企业自定义格式。
6. 支持深度道、曲线道、岩性道、地层道、文本道、符号道、取芯道和成像道等专业图道。
7. 支持多井排列、层位连接、地层拉平、井间距设置、砂体及沉积相充填、跨井选择和同步高亮。
8. 建立统一的图形与数据双向编辑机制，支持命令化操作、撤销重做、事务提交和修改审计。
9. 支持 PDF、SVG、CGM、TIFF、PNG 等多格式输出，实现单井长卷图和连井剖面图的工程级输出。
10. 建立 Native SDK 与 Python SDK，支持图道、算法、数据源、工具、模板、导出和界面模块的二次开发。
11. 形成完整的插件管理、版本兼容、依赖解析、权限控制、异常隔离和开发者文档体系。
12. 为储层评价、智能解释、多井综合分析和企业专业应用提供稳定扩展基础。

---

## 2. 设计原则

### 2.1 高性能与可扩展并重

高频曲线绘制、数据降采样、坐标转换、GPU 缓冲更新和图形拾取等高性能任务由 C++ 内核完成；业务流程、解释算法、工程管理和轻量界面扩展可由 Python 实现。平台通过稳定服务接口连接两类模块，避免把插件直接耦合到底层实现。

### 2.2 微内核与插件化

软件包内核只保留项目管理、数据管理、命令、事件、渲染、选择、模板、导出和设置等基础服务。专业业务能力以插件形式实现，包括平台自带模块也按照内置插件方式开发，以保证插件机制能够被系统自身持续验证。

独立桌面应用不复制或重写内核能力，而是通过公开的软件包接口组合单井、连井、项目管理和工作台功能。其他业务系统也可以使用相同接口嵌入单井画布、连井画布或后台数据处理能力。

### 2.3 分层真实数据源

工程与文档内容的权威在 **WellLog Document（及修订 Revision）**；所有持久化语义修改经 **Data Patch**（及后续产品化的 Command/Transaction 包装）提交，禁止扩展代码直接改写底层可写内存，以保证一致性、撤销重做与可审计性。

运行时曲线/区间等列式样点以引擎 **不可变自有缓冲区（owned buffers）** 为内存真源。**Apache Arrow**（C Data Interface / IPC / 可选适配层）是跨语言、零拷贝 **交换与互操作边界**，不是强制的唯一运行时载体。列式布局、批量计算与零拷贝目标不变；实现上允许 buffer 主路径 + Arrow 边界（见仓库 ADR 0006、0027）。

### 2.4 屏幕与输出统一

屏幕渲染和矢量导出共享同一场景布局模型、单位体系和绘制命令。OpenGL、PDF、SVG、CGM 和高分辨率栅格输出分别作为渲染后端，实现布局规则和几何表达的一致。

> **兑现分期（与 §15 轨 E 一致）**：愿景格式清单含 CGM 与工程级 WYSIWYG；**Desktop 首发（导出 B0）** 以 PDF/SVG/PNG/TIFF + 打印预览骨架 + 单井几何金标为过线；**CGM、可搜索 PDF、0.1 mm 全矩阵** 归 **导出 B1**，不在未选型前静默承诺已交付（ADR 0021 / 0047）。

### 2.5 接口稳定优先

插件接口采用版本化设计。跨模块数据优先使用 Arrow C Data Interface、标准 C ABI、JSON Schema 和稳定的领域对象标识，尽量减少第三方插件对编译器、Qt 内部类型和 C++ 标准库 ABI 的直接依赖。

### 2.6 跨平台与开源合规

正式发行版统一采用 Qt 6 与 PySide6。核心依赖优先选择 Apache、BSD、MIT、LGPL 等可用于跨平台工程交付的组件，并在安装包中提供第三方许可证清单、动态链接说明和源代码获取方式。

### 2.7 软件包与桌面应用分离

软件包和桌面应用采用明确的产品边界：

- 软件包负责“能力提供”，包括模型、数据、渲染、交互、输出、插件和 SDK；
- 桌面应用负责“产品组织”，包括工程入口、工作区、窗口布局、业务流程、用户操作和安装发布；
- 单井与连井使用同一 `WellModel`、数据服务、图道插件、命令体系和输出后端；
- 桌面应用仅依赖软件包公开 API，不访问软件包内部私有实现；
- 软件包可以无桌面界面运行，用于批处理、自动制图、服务化调用和第三方系统集成；
- 桌面应用可以通过插件增加新的图道、算法、面板和行业流程。


---

## 3. 总体技术架构

### 3.1 双产品形态

本项目形成两个相互独立但共享代码基线的交付物。

#### 3.1.1 测井绘图软件包

软件包可作为动态库、Python 包、Qt 控件包和 SDK 发布，主要包括：

- `wellplot-core`：工程模型、数据模型、单位和坐标系统；
- `wellplot-data`：Arrow 数据存储、格式读取和缓存；
- `wellplot-render`：单井与连井渲染内核；
- `wellplot-widgets`：可嵌入 Qt/PySide6 的单井和连井控件；
- `wellplot-services`：命令、事务、事件、模板和输出服务；
- `wellplot-plugin-runtime`：插件发现、加载和生命周期管理；
- `wellplot-sdk-cpp`：C++ 二次开发接口；
- `wellplot-sdk-python`：Python 二次开发接口；
- `wellplot-cli`：批处理、自动导出和工程检查工具。

#### 3.1.2 独立桌面应用

独立桌面应用是软件包的官方产品化实现，主要包括：

- 工程启动页和最近项目；
- 单井工程与连井工程管理；
- 单井绘图工作区；
- 连井地层对比工作区；
- 数据表、属性、图层、模板和任务面板；
- 菜单、工具栏、状态栏和快捷键；
- 插件管理器；
- 打印预览和成果导出；
- 日志、崩溃恢复和用户设置。

### 3.2 分层架构

```text
┌──────────────────────────────────────────────────────────────┐
│                 独立桌面应用 WellPlot Desktop               │
│ 项目中心 / 单井工作区 / 连井工作区 / Dock / 插件管理 / 输出 │
├──────────────────────────────────────────────────────────────┤
│                 Application Composition Layer                │
│ Workspace / Document / Window / Action / Workflow            │
├──────────────────────────────────────────────────────────────┤
│                 可复用测井绘图软件包                         │
│ Core / Data / Render / Widgets / Services / Plugin Runtime   │
├──────────────────────────────────────────────────────────────┤
│                    Application Services                      │
│ Project / Data / Command / Transaction / Event / Selection  │
│ Template / Render / Export / Settings / Logging / Task      │
├──────────────────────────────────────────────────────────────┤
│                        Core Kernel                           │
│ WellModel / MultiWellModel / Arrow Store / Scene / Layout   │
│ Viewport / Unit / Coordinate / Data Patch / Resource        │
├──────────────────────────────────────────────────────────────┤
│                     Rendering Backends                       │
│ OpenGL / PDF / SVG / CGM / Raster / Print                   │
├──────────────────────────────────────────────────────────────┤
│                     Storage and I/O                          │
│ Arrow IPC / SQLite / Memory Mapping / File System / Cache    │
└──────────────────────────────────────────────────────────────┘
```

### 3.3 软件包嵌入方式

软件包支持以下集成形式：

1. C++ 应用通过动态库和 C++ SDK 嵌入；
2. PySide6 应用通过 Python 包和原生 QWidget 控件嵌入；
3. 其他 Qt 应用直接嵌入 `SingleWellWidget` 和 `MultiWellCorrelationWidget`；
4. 无界面程序通过 CLI 或服务接口完成数据转换和批量导出；
5. 第三方系统通过插件扩展图道、格式、算法和导出能力。

### 3.4 技术栈

| 层级 | 推荐技术 | 主要职责 |
|---|---|---|
| 核心语言 | C++20 | 数据内核、渲染、交互、任务调度 |
| 桌面框架 | Qt 6.x | 独立桌面程序、窗口、Dock、菜单和事件 |
| Python 集成 | PySide6 | Python SDK、业务扩展和轻量界面 |
| C++/Python 绑定 | pybind11 / Shiboken6 | 官方软件包绑定层 |
| 图形渲染 | OpenGL 3.3 Core | 单井和连井高性能图形绘制 |
| 数据内存 | Apache Arrow | 列式存储、零拷贝交换和批量计算 |
| 工程数据库 | SQLite | 工程元数据、文档索引、模板和日志 |
| 矢量输出 | Cairo / Skia / 自研后端 | PDF、SVG、CGM 和打印输出 |
| 构建系统 | CMake | 软件包和桌面应用统一构建 |
| 包管理 | Conan/vcpkg | C++ 依赖管理 |
| Python 发布 | wheel | Python 软件包和 SDK 发布 |
| 测试框架 | Catch2/GoogleTest + pytest | 内核、桌面、接口和插件测试 |
| 持续集成 | GitHub Actions/GitLab CI | 双平台构建、测试和安装包发布 |

---

## 4. 核心领域模型

### 4.1 工程模型

```text
Project
 ├── Wells
 │    └── Well
 │         ├── WellMetadata
 │         ├── Trajectory
 │         ├── CurveSet
 │         ├── FormationSet
 │         ├── LithologySet
 │         ├── AnnotationSet
 │         └── InterpretationSet
 ├── Documents
 │    ├── SingleWellDocument
 │    └── MultiWellCorrelationDocument
 ├── CorrelationSets
 ├── Templates
 ├── Resources
 └── AuditLogs
```

一个工程可以包含多口井、多个单井文档和多个连井文档。单井文档引用一口井及其绘图模板；连井文档引用多口井、井序、井间距、对比基准、层位连接和井间填充对象。

### 4.2 单井数据对象

- `WellModel`：单井基础信息、井位、井深和轨迹。
- `CurveChannel`：测井曲线及其深度域、单位、采样方式和空值定义。
- `ImageLog`：FMI、STAR 等成像测井二维矩阵。
- `FormationTop`：地层顶底界、名称、层级和解释属性。
- `LithologyInterval`：岩性区间、岩性代码、比例和填充规则。
- `Annotation`：文本、符号、区间和解释标注。
- `TrackDefinition`：图道类型、位置、宽度、坐标轴和样式。
- `SingleWellDocument`：单井图件布局、图道列表、图头图尾和打印参数。

### 4.3 连井数据对象

- `MultiWellCorrelationDocument`：连井文档、井序、视口和输出参数。
- `WellColumnDefinition`：一口井在连井剖面中的位置、宽度和显示模板。
- `CorrelationHorizon`：跨井同名层位及各井控制点。
- `CorrelationLink`：相邻井之间的层位连接线。
- `FlattenReference`：连井地层拉平所使用的基准层位。
- `InterWellBody`：相邻井间砂体、储层或沉积相充填对象。
- `WellSpacingModel`：实际井间距、等间距或自定义水平位置。
- `CorrelationAnnotation`：跨井文本、构造说明和解释标注。
- `CorrelationTemplate`：连井图头、井列、层位样式和充填规则。

### 4.4 公共数据对象

- `PlotDocument`：单井和连井文档的公共抽象。
- `DataPatch`：对数据对象的增量修改描述。
- `CommandRecord`：可撤销操作、来源插件、操作时间和审计信息。
- `SelectionSet`：单井图元、跨井层位和井间对象的统一选择集合。
- `StyleResource`：线型、字体、颜色、符号和岩性花纹。
- `ExportProfile`：输出格式、比例尺、纸张和字体配置。

### 4.5 深度域与对比坐标

平台支持：

- MD：测量深度；
- TVD：垂直深度；
- TVDSS：海拔基准垂直深度；
- 自定义深度参考；
- 非均匀采样；
- 不同曲线独立深度数组；
- 深度转换和轨迹投影；
- 连井统一基准深度；
- 按指定层位拉平；
- 每口井独立纵向变换；
- 连井剖面实际距离或等间距排列。

不同曲线不得为了统一采样而强制插值。连井显示也不得修改原始单井深度数据，而应通过文档级坐标变换建立对比视图。

---

## 5. 数据与缓存架构

### 5.1 三级数据流

```text
一级：磁盘与内存映射
Arrow IPC / Feather / 原始测井文件 / mmap

二级：Arrow Columnar Store
作为运行时单一真实数据源，保存原始数据和修改版本

三级：GPU Resource Cache
保存当前视口所需的曲线顶点、纹理和图元缓存
```

### 5.2 数据访问接口

插件只能通过以下受控接口访问数据：

```cpp
class IDataService {
public:
    virtual DataView openReadOnlyView(
        const DataObjectId& objectId) = 0;

    virtual DataPatchBuilder createPatch(
        const DataObjectId& objectId) = 0;

    virtual SubmitResult submit(
        const DataPatch& patch,
        const TransactionOptions& options) = 0;
};
```

其中：

- `DataView` 为只读视图；
- `DataPatchBuilder` 用于构造局部增量修改；
- `submit` 负责校验、事务写入、审计记录和事件发布；
- 插件不得获取 Arrow 可写缓冲区的长期所有权。

### 5.3 数据修改流程

```text
插件读取只读 DataView
        ↓
生成 DataPatch
        ↓
提交 CommandService
        ↓
执行数据与单位校验
        ↓
TransactionService 事务写入
        ↓
生成 Undo/Redo 记录和审计日志
        ↓
发布 DataChangedEvent
        ↓
更新视口相关 GPU 缓存
```

---

## 6. 渲染架构

### 6.1 公共渲染内核

单井和连井不建设两套渲染引擎，而是共享以下公共能力：

- 场景布局；
- 深度坐标变换；
- 图道渲染器；
- 设备无关绘制命令；
- 图形拾取；
- 字体和符号资源；
- OpenGL、PDF、SVG、CGM 和栅格后端；
- 选择、高亮和编辑反馈；
- LoD 和 GPU 缓存。

### 6.2 单井画布

每个单井绘图文档使用一个 `SingleWellCanvas`，内部管理多个逻辑图道，不为每个图道创建独立 `QOpenGLWidget`。

```text
SingleWellCanvas
 ├── SceneLayout
 ├── SingleWellViewport
 ├── TrackContainer
 │    ├── DepthTrackRenderer
 │    ├── CurveTrackRenderer
 │    ├── LithologyTrackRenderer
 │    ├── FormationTrackRenderer
 │    ├── ImageTrackRenderer
 │    └── ThirdPartyTrackRenderer
 ├── PickingService
 ├── RenderCommandBuffer
 └── RenderBackend
```

单井画布负责：

- 单井图道编排；
- 深度平移和缩放；
- 精确比例尺；
- 曲线和区间编辑；
- 图头图尾；
- 长卷输出；
- 单井解释成果展示。

### 6.3 连井画布

连井文档使用一个 `MultiWellCorrelationCanvas`。画布由多个逻辑井列组成，每个井列复用单井图道渲染器，但所有井列由同一个场景、视口和渲染后端统一调度。

```text
MultiWellCorrelationCanvas
 ├── CorrelationSceneLayout
 ├── CorrelationViewport
 ├── WellColumnContainer
 │    ├── WellColumn A
 │    ├── WellColumn B
 │    ├── WellColumn C
 │    └── ...
 ├── HorizonLinkLayer
 ├── InterWellFillLayer
 ├── CorrelationAnnotationLayer
 ├── PickingService
 ├── RenderCommandBuffer
 └── RenderBackend
```

连井画布支持：

- 多井水平排列和拖拽排序；
- 实际井间距、等间距和自定义间距；
- 各井独立图道模板；
- 统一深度浏览；
- 指定层位拉平；
- 跨井层位连接；
- 连接点交互修改；
- 砂体、储层和沉积相井间充填；
- 跨井选择和同步高亮；
- 单井与连井视图定位联动；
- 连井剖面的分页或大幅面输出。

### 6.4 设备无关绘制命令

普通图道插件不直接调用 OpenGL，而是生成设备无关绘制命令。

```cpp
class ITrackRenderer {
public:
    virtual void prepare(
        const TrackContext& context,
        const DataView& data) = 0;

    virtual void buildRenderCommands(
        const VisibleDepthRange& range,
        IRenderCommandSink& sink) = 0;

    virtual HitResult hitTest(
        const LogicalPoint& point) const = 0;
};
```

`IRenderCommandSink` 提供：

- 折线和多段线；
- 多边形；
- 文本；
- 图像；
- 纹理与花纹；
- 符号；
- 裁剪区域；
- 透明度和混合模式；
- 单井深度坐标变换；
- 连井井列坐标和层位连接坐标变换。

OpenGL、PDF、SVG、CGM 和栅格后端分别解释相同绘制命令。

### 6.5 原生高性能渲染扩展

对于成像测井、三维井轨迹等特殊模块，可提供 `INativeRenderPass`。该接口必须使用宿主分配的 GPU 资源、Shader 和 Render Pass，禁止插件直接修改全局 OpenGL 状态。

### 6.6 坐标与比例尺

平台建立统一坐标体系：

- 深度逻辑坐标；
- 图道局部坐标；
- 单井文档坐标；
- 连井井列坐标；
- 连井剖面坐标；
- 文档物理毫米坐标；
- 屏幕像素坐标；
- 打印设备坐标。

单井纵向缩放受测井物理比例尺约束。连井文档可使用统一比例尺，也可通过层位拉平形成文档级非线性显示变换，但原始深度数据保持不变。

### 6.7 数据降采样

曲线渲染根据视口范围自适应选择：

- 原始点直接绘制；
- Min-Max 像素桶抽稀；
- LTTB 关键点抽稀；
- 多级缓存 LoD；
- 后台线程计算；
- 主线程提交 GPU 资源。

连井模式按井列和可见范围分别建立缓存，并共享相同曲线数据块，避免相同井数据重复加载。降采样结果只用于屏幕显示，矢量导出和数据分析默认使用原始数据。

---

## 7. 独立桌面应用与专业功能模块

### 7.1 桌面应用总体设计

独立桌面应用暂定名为 `WellPlot Desktop`，通过系统安装包独立部署。其主界面采用多文档和可停靠面板结构。

```text
WellPlot Desktop
 ├── Project Center
 ├── Main Window
 │    ├── Menu and Toolbar
 │    ├── Document Area
 │    ├── Project Explorer
 │    ├── Data and Layer Panel
 │    ├── Property Panel
 │    ├── Template Panel
 │    ├── Task and Log Panel
 │    └── Status Bar
 ├── Single-Well Workspace
 ├── Multi-Well Workspace
 ├── Plugin Manager
 └── Print and Export Center
```

桌面应用支持：

- 新建和打开单井、连井工程；
- 在同一工程中同时打开多个单井和连井文档；
- 文档标签、分屏和浮动窗口；
- 工作区布局保存；
- 最近项目和自动恢复；
- 统一撤销、重做和操作历史；
- 插件安装、启用、禁用和诊断；
- 用户偏好、快捷键和单位设置；
- 批量输出和任务进度管理。

### 7.2 单井工作区

单井工作区面向一口井的绘图、编辑和解释，主要包括：

- 单井数据导入和曲线管理；
- 图道模板选择和编辑；
- 深度道、曲线道、岩性道、地层道、文本道、取芯道、成像道和符号道；
- MD、TVD、TVDSS 切换；
- 曲线预处理和计算；
- 地层和岩性区间编辑；
- 曲线交会填充；
- 图形与数据表同步定位；
- 图头图尾和图例；
- 单井长卷打印；
- 单井模板保存及在其他井上复用。

### 7.3 连井工作区

连井工作区面向多井地层对比和剖面成果编制，主要包括：

- 从项目井列表选择多口井；
- 调整井序和井列宽度；
- 实际井间距、等间距和自定义间距；
- 每口井选择单井显示模板；
- 统一深度范围和比例尺；
- 选择某一地层作为拉平基准；
- 自动或手动建立跨井同名层位；
- 拖动修改层位连接点；
- 显示不整合、断层和尖灭关系；
- 在相邻井之间绘制砂体、储层和沉积相充填；
- 跨井图元选取和同步高亮；
- 双击井列跳转到对应单井工作区；
- 单井地层修改后刷新连井对比结果；
- 输出大幅面连井剖面图和分页报告。

### 7.4 单井与连井联动

单井和连井文档基于同一项目数据，不复制解释成果。

联动规则包括：

1. 单井工作区修改地层顶界后，引用该层位的连井文档收到数据变更事件；
2. 连井工作区拖动某井的层位控制点时，通过 Command 和 Transaction 更新对应单井地层数据；
3. 在连井工作区选择某井、曲线或层段时，可在单井文档中同步定位；
4. 单井模板可以作为连井井列模板使用；
5. 单井岩性、解释结论和储层区间可以直接参与井间充填；
6. 单井和连井编辑均进入统一 Undo/Redo 和审计体系；
7. 文档可选择自动刷新或提示用户确认刷新。

### 7.5 数据管理与预处理

支持：

- LAS 2.0/3.0；
- DLIS/RP66；
- LIS；
- 716；
- ASCII、CSV、Excel；
- Arrow IPC、Feather；
- 企业数据库和自定义格式插件。

预处理功能包括：

- 深度平移和非线性校正；
- 主测、复测对齐；
- 高斯、中值、滑动平均等滤波；
- 异常点检测与修复；
- 单位换算；
- 基线校正；
- 曲线合并、裁剪和重采样；
- 区间统计和批量提取。

### 7.6 图道与模板

内置图道包括：

1. 深度道；
2. 曲线道；
3. 岩性道；
4. 地层道；
5. 文本道；
6. 取芯道；
7. 成像道；
8. 符号道。

图道支持拖拽排序、宽度调整、显示隐藏、分组嵌套、多曲线叠加、线性和对数轴、区间填充、曲线交会填充以及样式继承。模板分为单井模板、连井井列模板和连井剖面模板。

### 7.7 图头图尾

支持可视化配置：

- 井名、井号和位置；
- 坐标和构造位置；
- 测井项目表；
- 比例尺；
- 图例；
- 编制、审核和签署栏；
- 单位标识；
- 企业 Logo；
- 自定义表格；
- 连井井位和井间距标注；
- 模板变量。

### 7.8 图形编辑

支持：

- 地层线拖动；
- 岩性区间顶底界调整；
- 曲线徒手修改；
- 橡皮擦和局部修线；
- 文本、符号和区间标注；
- 取芯点添加；
- 测试段和压裂段编辑；
- 跨井层位连接编辑；
- 井间充填边界编辑；
- 多对象选择；
- 对齐、吸附和批量修改；
- 撤销、重做和修改日志。

### 7.9 导出与打印

支持：

- 单井连续长卷 PDF；
- 连井大幅面 PDF；
- 分页 PDF；
- SVG；
- CGM；
- TIFF、PNG、JPEG；
- 300—1200 DPI 栅格导出；
- 长卷打印机和大幅面绘图仪；
- 页面预览；
- 物理毫米布局；
- 字体嵌入；
- 图层和裁剪；
- 企业输出模板。

---

## 8. 插件体系

### 8.1 插件类型

| 插件类型 | 接口 | 主要用途 |
|---|---|---|
| 数据源插件 | `IDataSourcePlugin` | LAS、DLIS、自定义格式和数据库 |
| 数据处理插件 | `IDataProcessorPlugin` | 滤波、校正、换算和曲线计算 |
| 图道插件 | `ITrackPlugin` | 专业图道和企业自定义图道 |
| 图元插件 | `IGraphicItemPlugin` | 地层线、符号、标注和解释图元 |
| 工具插件 | `IToolPlugin` | 拾取、修线、量测和区间编辑 |
| 分析插件 | `IAnalysisPlugin` | 储层评价、岩性识别和交会分析 |
| 模板插件 | `ITemplateProvider` | 行业模板和企业模板 |
| 导出插件 | `IExportPlugin` | PDF、SVG、CGM 和企业格式 |
| 工作台插件 | `IWorkbenchPlugin` | Dock、菜单、属性页和向导 |

### 8.2 宿主服务接口

所有插件通过 `IHostServices` 获取宿主能力。

```cpp
class IHostServices {
public:
    virtual IProjectService* project() = 0;
    virtual IDataService* data() = 0;
    virtual ICommandService* commands() = 0;
    virtual ITransactionService* transactions() = 0;
    virtual IEventBus* events() = 0;
    virtual ISelectionService* selection() = 0;
    virtual IRenderService* rendering() = 0;
    virtual ITemplateService* templates() = 0;
    virtual IExportService* exportService() = 0;
    virtual ISettingsService* settings() = 0;
    virtual ILogger* logger() = 0;
};
```

插件不得直接访问：

- 主窗口私有对象；
- SQLite 原始连接；
- Arrow 可写内存；
- OpenGL 全局 Context；
- 全局单例；
- 其他插件内部对象。

### 8.3 插件生命周期

统一生命周期包括：

```text
发现 discover
校验 validate
解析依赖 resolve
加载 load
初始化 initialize
激活 activate
停用 deactivate
卸载 unload
```

插件加载失败不得影响宿主程序启动。插件运行异常应记录到独立日志，并由插件管理器提供停用、重试和安全模式启动能力。

### 8.4 插件包

插件包采用 `.rfplugin` 格式。

```text
com.example.custom-lithology.rfplugin
 ├── manifest.json
 ├── bin/
 │    ├── windows-x64/plugin.dll
 │    └── linux-x64/libplugin.so
 ├── python/
 ├── resources/
 ├── schemas/
 ├── translations/
 ├── LICENSE
 └── README.md
```

示例 `manifest.json`：

```json
{
  "id": "com.example.custom-lithology",
  "name": "Custom Lithology Track",
  "version": "1.2.0",
  "apiVersion": "1.0",
  "entryType": "native",
  "entryPoint": "CustomLithologyPlugin",
  "capabilities": [
    "track.lithology",
    "symbol.provider"
  ],
  "dependencies": {
    "wellplot.core": ">=1.0,<2.0"
  },
  "platforms": [
    "windows-x64",
    "linux-x64"
  ],
  "permissions": [
    "project.read",
    "project.write"
  ]
}
```

### 8.5 版本兼容

平台采用以下版本策略：

- 插件版本遵循语义化版本；
- `apiVersion` 表示宿主插件 API 大版本；
- 同一 API 大版本内保持源代码兼容；
- C ABI 采用结构体大小和版本字段进行兼容；
- Qt UI 插件必须使用官方指定工具链构建；
- Python 插件声明最低 Python 和 SDK 版本；
- 每个正式版本发布插件兼容性矩阵；
- CI 自动运行插件契约测试。

---

## 9. 二次开发 SDK

### 9.1 Native SDK

Native SDK 包括：

```text
wellplot-sdk-c
wellplot-sdk-cpp
wellplot-sdk-qt
```

主要能力：

- 插件生命周期；
- 宿主服务访问；
- Arrow 数据交换；
- 图道渲染命令；
- 命令与事务；
- 事件订阅；
- 日志；
- 设置；
- 资源和国际化；
- 原生 Dock 与属性页扩展。

高性能模块优先使用 Native SDK。

### 9.2 Python SDK

Python SDK 包结构：

```text
wellplot_sdk
 ├── plugin
 ├── project
 ├── data
 ├── commands
 ├── events
 ├── tracks
 ├── analysis
 ├── ui
 ├── export
 └── testing
```

Python 插件通过标准 entry points 自动发现：

```toml
[project.entry-points."wellplot.plugins"]
my_analysis = "my_analysis.plugin:AnalysisPlugin"
```

Python SDK 适用于：

- 数据处理算法；
- 解释流程；
- 统计分析；
- 轻量图道；
- 工具面板；
- 工程批处理；
- 自动化脚本；
- AI 模型调用。

### 9.3 插件脚手架

提供命令行工具：

```bash
wellplot-plugin create
wellplot-plugin build
wellplot-plugin test
wellplot-plugin package
wellplot-plugin install
```

脚手架可生成：

- C++ 图道插件；
- Python 分析插件；
- 数据源插件；
- Dock 面板插件；
- 导出插件；
- 测试模板；
- Manifest；
- CI 配置。

### 9.4 开发者文档

开发者文档至少包括：

- 架构说明；
- 插件快速入门；
- API Reference；
- 图道插件教程；
- 数据源插件教程；
- Python 算法插件教程；
- 调试指南；
- 性能优化指南；
- 兼容性说明；
- 发布与签名流程；
- 示例插件源码。

---

## 10. 事件、命令与事务

### 10.1 事件总线

平台通过事件总线解耦模块。

典型事件包括：

- `ProjectOpenedEvent`；
- `WellActivatedEvent`；
- `ViewportChangedEvent`；
- `SelectionChangedEvent`；
- `DataChangedEvent`；
- `TemplateAppliedEvent`；
- `ExportCompletedEvent`；
- `PluginStateChangedEvent`。

事件只携带稳定标识和不可变数据快照，避免插件之间共享可变对象。

### 10.2 命令机制

所有会修改工程状态的操作必须实现为 Command：

```cpp
class ICommand {
public:
    virtual CommandResult execute() = 0;
    virtual CommandResult undo() = 0;
    virtual CommandResult redo() = 0;
    virtual std::string description() const = 0;
};
```

命令服务统一处理：

- Undo/Redo；
- 命令合并；
- 宏命令；
- 批量操作；
- 审计日志；
- 插件来源记录；
- 崩溃恢复。

### 10.3 事务

一个命令可以包含多个 Data Patch。事务保证：

- 全部成功或全部回滚；
- 数据校验；
- 单位和深度范围校验；
- 版本冲突检测；
- 操作日志；
- 事件延迟发布；
- GPU 缓存统一刷新。

---

## 11. 工程文件与存储

建议工程目录结构：

```text
project.wlp/
 ├── project.json
 ├── metadata.db
 ├── data/
 │    ├── curves.arrow
 │    ├── images.arrow
 │    ├── formations.arrow
 │    └── interpretations.arrow
 ├── documents/
 │    ├── single-well/
 │    └── multi-well/
 ├── correlations/
 ├── templates/
 │    ├── single-well/
 │    └── multi-well/
 ├── resources/
 ├── cache/
 ├── audit/
 └── thumbnails/
```

其中：

- `project.json` 保存工程基本描述、工程类型和版本；
- `metadata.db` 保存井、文档、对象索引、插件状态和日志索引；
- Arrow 文件保存多口井的大规模列式数据；
- `documents/single-well` 保存单井文档配置；
- `documents/multi-well` 保存连井文档、井序、拉平和连接配置；
- `correlations` 保存跨井层位关系和井间充填对象；
- 模板按单井与连井分类管理；
- Cache 可删除和重建；
- Audit 保存单井和连井的统一操作日志和恢复信息。

工程升级采用显式 Migration，不允许插件自行修改数据库结构。桌面应用、软件包和批处理工具必须通过同一工程读写服务访问项目。

---

## 12. 仓库与代码组织

```text
wellplot/
├── apps/
│   ├── desktop/
│   │   ├── project-center/
│   │   ├── single-well-workspace/
│   │   ├── multi-well-workspace/
│   │   ├── plugin-manager/
│   │   └── export-center/
│   └── cli/
├── packages/
│   ├── core/
│   ├── data/
│   ├── rendering/
│   ├── widgets/
│   ├── services/
│   └── plugin-runtime/
├── core/
│   ├── model/
│   ├── data/
│   ├── command/
│   ├── transaction/
│   ├── event/
│   ├── layout/
│   ├── units/
│   └── coordinate/
├── rendering/
│   ├── api/
│   ├── single-well/
│   ├── multi-well/
│   ├── opengl/
│   ├── vector/
│   ├── raster/
│   └── picking/
├── widgets/
│   ├── single-well-widget/
│   ├── multi-well-correlation-widget/
│   └── property-editors/
├── plugin-runtime/
│   ├── manifest/
│   ├── loader/
│   ├── registry/
│   ├── dependency/
│   ├── permissions/
│   └── sandbox/
├── services/
│   ├── project/
│   ├── data/
│   ├── correlation/
│   ├── template/
│   ├── export/
│   └── settings/
├── sdk/
│   ├── c/
│   ├── cpp/
│   ├── qt/
│   └── python/
├── plugins/
│   ├── data-las/
│   ├── data-dlis/
│   ├── track-depth/
│   ├── track-curve/
│   ├── track-lithology/
│   ├── track-formation/
│   ├── track-image/
│   ├── correlation-horizon/
│   ├── correlation-fill/
│   ├── tool-curve-editor/
│   └── export-pdf/
├── examples/
│   ├── embed-single-well/
│   ├── embed-multi-well/
│   ├── hello-track/
│   ├── custom-parser/
│   ├── python-analysis/
│   └── dock-panel/
├── tests/
│   ├── core/
│   ├── desktop/
│   ├── single-well/
│   ├── multi-well/
│   ├── rendering/
│   ├── plugin-contract/
│   ├── compatibility/
│   └── performance/
└── docs/
    ├── architecture/
    ├── desktop-user-guide/
    ├── sdk/
    ├── embedding-guide/
    ├── plugin-guide/
    └── api-reference/
```

单井画布、连井画布、LAS、DLIS、曲线道、岩性道、层位连接和井间充填均应基于公共软件包接口实现。独立桌面应用不得形成与软件包平行的私有渲染和数据逻辑。

---

## 13. 跨平台与发布

### 13.1 支持平台

首期支持：

- Windows 10/11 x64；
- Ubuntu LTS x64；
- 国产 Linux x64，可根据项目环境适配统信 UOS、麒麟等系统；
- NVIDIA、AMD、Intel 集成显卡；
- OpenGL 3.3 Core Profile。

### 13.2 构建矩阵

| 平台 | 编译器 | Python | Qt |
|---|---|---|---|
| Windows | MSVC 2022 | 官方指定版本 | Qt 6.x |
| Linux | GCC/Clang | 官方指定版本 | Qt 6.x |
| 国产 Linux | GCC/Clang | 官方指定版本 | Qt 6.x |

### 13.3 发布内容

项目分别发布软件包和桌面应用。

#### 软件包发布物

- C++ 动态库和头文件；
- `SingleWellWidget`；
- `MultiWellCorrelationWidget`；
- Python wheel；
- CLI 工具；
- 内置基础插件；
- C++/Python SDK；
- 嵌入示例；
- API 与集成文档。

#### 桌面应用发布物

- 独立可执行桌面主程序；
- Windows/Linux 安装包；
- 单井工作区；
- 连井工作区；
- 核心动态库和内置插件；
- Python 运行环境；
- 插件管理器；
- 示例单井和连井项目；
- 用户手册；
- 第三方许可证清单；
- 崩溃恢复、诊断和日志工具。

---

## 14. 安全与稳定性

### 14.1 插件权限

插件在 Manifest 中声明权限：

- `project.read`；
- `project.write`；
- `filesystem.read`；
- `filesystem.write`；
- `network.access`；
- `native.render`；
- `ui.extend`；
- `process.execute`。

高风险权限默认关闭，并由管理员或用户确认。

### 14.2 插件隔离

首期至少实现：

- 加载异常捕获；
- 插件独立日志；
- 安全模式启动；
- 插件启用与禁用；
- 超时任务取消；
- 后台任务异常隔离；
- Python 插件独立解释环境规划；
- 插件签名校验接口。

对于不可信 Python 算法插件，可在后续版本增加独立进程运行模式。

### 14.3 OpenGL 安全

- 所有 OpenGL 调用限制在拥有 Context 的渲染线程；
- 后台线程只计算数据和构造中间结果；
- GPU 资源由宿主管理；
- 插件不能跨线程调用 OpenGL；
- 插件卸载时统一释放 GPU 资源；
- 使用状态保护和 Render Pass 防止插件污染全局状态。

---

## 15. 实施计划

> **文档角色（2026-08 共识）**：本章与全文为 **北极星愿景**，不是逐字 as-built 规格。  
> 已实现能力以仓库 `well-log-engine/`、ADR `docs/adr/0004+`、`well-log-engine/docs/prd-143-completion.md` 为准。  
> 下文用 **现状快照 + 已决策偏离 + 多轨计划** 与愿景对齐，避免阶段编号与代码脱节。

### 15.1 现状快照（相对愿景阶段 0–6）

| 愿景阶段 | 主题 | 状态 | 仓库落点（摘要） |
|---|---|---|---|
| 0 | 产品边界、架构、SDK 基线 | **基本完成** | 独立 `well-log-engine` CMake/package consumer；大量 ADR；双平台 CI（Windows ctest 仍有 advisory 项，见 #286 等） |
| 1 | 数据内核与单井软件包 | **引擎侧基本完成** | Session/Scene/RenderGL/`WellLogView`、LOD、深度变换、可选 Arrow、LAS/DLIS/LIS/716；命名为 WellLog* 而非 wellplot-* |
| 2 | 单井桌面工作区 | **部分完成** | `well_log_workstation` 壳 + 模板/多轨/图件；非完整工程中心/八类图道/安装包 Desktop |
| 3 | 编辑、命令、事务 | **半完成** | 引擎 DocumentPatch + Undo/Redo + 表选择联动；**宿主级 Command 总线 / 审计 / 崩溃恢复未做**（当前轨不强制） |
| 4 | 连井软件包与工作区 | **基础有、产品薄** | 多井 surface、overlay、depth transform；井间砂体/沉积相充填、完整连井工程流不足 |
| 5 | 输出与打印 | **内核厚、产品未齐** | 引擎 PDF/SVG/PNG/TIFF；单井引擎导出绑定（Stage 1）；CGM/打印预览/全量 WYSIWYG 未齐 |
| 6 | 二次开发与产品化 | **未开始（刻意收缩）** | SDK 0.1 级；无完整插件 Runtime/脚手架/Desktop 安装升级体系 |

**引擎 PRD #143 phase-one 已关闭**：软件包能力大体覆盖愿景阶段 0–1 及 3–5 的内核切片；缺口集中在 **Desktop 发行、单井/连井产品工作区、导出 B0→B1 兑现**。

### 15.2 已决策偏离（愿景句 vs 现行 ADR）

| 主题 | 愿景原文倾向 | 现行决策 | 文档处理 |
|---|---|---|---|
| 运行时真源 | Arrow 唯一内存真源 | Document+Patch 权威；owned buffer 运行时；Arrow 为交换边界 | §2.3 已改写为分层真源 |
| 交互 GL 回退 | 曾写软件降级 | **禁止** 交互路径软件 GL/QPainter 回退（ADR 0033） | §17.2 已同步 |
| 模块命名 | wellplot-* | `WellLog::*` / `well-log-engine` | 愿景保留产品名 WellPlot；实现名以仓库为准 |
| 控件命名 | SingleWellWidget 等 | `WellLogView` + 宿主 canvas | 能力映射，不强制改历史 API 名 |
| 插件体系 | 完整 Runtime | 首发 **Custom Layer + SDK 嵌入**；完整 Runtime 后置 | 见轨 P（后置） |
| CGM | 阶段 5 清单 | 首期不做（ADR 0021）；进导出 **B1** | §2.4 脚注 + 轨 E |
| 引擎 PDF 文本 | 工程级输出 | 手写 PDF 为轮廓字、不可搜索（ADR 0047）；须 UI 披露 | 轨 E B0 披露 / B1 可搜索路径 |
| Desktop 代码 | 另起 WellPlot Desktop | **`well_log_workstation` 升格** 为 Desktop 发行物 | 轨 D |
| Workbench | 同级产品 | Paleo Workbench **嵌入为次要路径**；主验收在独立 Desktop | 轨 D |

### 15.3 当前执行策略：三轨并行 + 后置轨

当前不按「严格串行阶段 2→3→4→5」推进，而采用：

| 轨 | 名称 | 目标 | 与愿景阶段关系 |
|---|---|---|---|
| **F** | 功能轨：单井 + 连井并行 | 可验收的单井工作区完整度 + 连井对比工作区（井序/间距/连接/拉平/充填） | 阶段 2 + 4 并行 |
| **D** | 发行轨：WellPlot Desktop | 独立安装包、启动页、升级/卸载；Workstation 升格品牌化 | 阶段 2/6 的产品化切片 |
| **E** | 导出轨：B0 → B1 | 先工程可用输出，再 CGM/可搜索 PDF/全量几何验收 | 阶段 5 分期 |
| **P**（后置） | 插件 Runtime / 宿主审计 / SDK 1.0 全套 | 不挡 F/D/E 首发 | 阶段 3 后半 + 阶段 6 大部 |
| **W**（后置） | Workbench 深度集成 | 嵌入与双开一致性，次于 Desktop 主验收 | 集成项 |

依赖原则：

- 轨 F 单井解释成果必须能被连井文档 **引用/刷新**（事件或显式重载），避免两套地层真源。
- 轨 D 不 fork 第二套画布；在 `well_log_workstation` 上升格。
- 轨 E 的 B0 为 Desktop 首发门禁之一；B1 可紧随但可落后于首个可安装版本一个小版本。
- 编辑过线采用 **引擎编辑面**（Patch/Undo/图表面一致）；宿主 Command 总线与审计日志属轨 P。

### 15.4 轨 F — 单井与连井功能（并行）

#### F1 单井工作区补齐

主要任务：

- 单井项目树 / 图道与属性面板（相对现壳补齐，不必一次做满八类若资源冲突，但须列出内置优先级）；
- 岩性、地层、文本、符号、取芯、成像等图道：引擎层已有 interval/pattern/text/image/custom 的，宿主接线与模板化；
- 单井模板与图头图尾最小可用集；
- 单井图件/工程保存与加载（宿主 Plot Revision 等已有机制上收敛 UX）。

交付物：

- Desktop 内可完成「打开井数据 → 组道 → 浏览编辑 → 保存」主路径；
- 与引擎 `WellLogView` / Session 的稳定嵌入。

#### F2 连井工作区推进（与 F1 并行）

主要任务：

- 连井文档模型与井序、井间距 UX；
- 层位连接、地层拉平（datum）；
- 井间砂体 / 沉积相充填（可先矢量/区间填充 MVP）；
- 单井 → 连井成果刷新；连井控制点回写单井走 **引擎 Patch**（无宿主事务总线）；
- 多井模板与示例工程。

交付物：

- Desktop 内可完成「多井排列 → 连接/拉平 → 导出 B0 连井输出」主路径；
- 不复制单井渲染内核（复用引擎 multi-well surface / 宿主 correlation 路径）。

#### F 轨验收（首发门禁）

- 单井：≥ 约定规模曲线可流畅浏览；模板可复用；保存/再开一致。
- 连井：≥ 20 井场景以「可交互」为目标做分级（性能绝对 FPS 仍按 §16 固定硬件另测）。
- 单井地层修改后相关连井视图可更新；连井深度控制点修改可撤销（引擎 Undo）。

### 15.5 轨 D — WellPlot Desktop 发行

主要任务：

- 产品命名与窗口/进程品牌：WellPlot Desktop（代码模块可暂名 `well_log_workstation`，渐进更名）；
- 工程启动页与最近项目；
- Windows / Linux **独立安装包**（升级、卸载）；
- 日志与基础用户设置；崩溃恢复 **不** 作为首发硬门（属轨 P）。
- Paleo Workbench 嵌入保持可用但不作为 Desktop 首发阻断项。

交付物：

- 可安装、可启动、可卸载的 Desktop 构建；
- 发行说明与第三方许可证清单（与引擎 SBOM 文档衔接）。

### 15.6 轨 E — 导出 B0 / B1

#### 导出 B0（Desktop 首发硬门）

- 单井：引擎路径 SVG + PDF（物理分页/比例按已交付能力）+ PNG；引擎 PDF **不可搜索文字须 UI 披露**；
- 连井：至少 PDF 或 SVG 之一 + PNG/TIFF 高分栅格；允许在引擎路由未齐时暂用已有后端，但须在发行说明标明路径；
- 打印预览 **骨架**（版心/页数/比例尺可读）；
- 单井主路径几何金标测试（屏幕 vs 导出，指标向 §16 0.1 mm 收敛，首发可先固定数据集子集）。

#### 导出 B1（紧随版本，兑现愿景阶段 5 全量）

- CGM 后端（新 ADR 选型后实现；在此之前不得宣称已支持）；
- 可搜索 PDF 路径（换栈或双后端；与 ADR 0047 显式修订）；
- 连井大幅面 + 0.1 mm 级一致性全矩阵；
- 长卷/大幅面打印与完整 WYSIWYG 报告。

### 15.7 后置轨 P / W（不挡首发）

| 项 | 说明 |
|---|---|
| 完整插件 Runtime | 发现/加载/版本/权限/隔离/管理 UI；首发仅 Custom Layer + 嵌入 SDK |
| 宿主 Command / Transaction / 审计日志 | 引擎 Patch 已够编辑门禁；审计与崩溃恢复后置 |
| SDK 1.0 与三方插件兼容 CI | 随 Runtime 一起 |
| Workbench 默认路径深度对等 | 保持双开，验收优先级低于 Desktop |
| 测井相（electrofacies）库内统计分类 | 研究结论：现无 in-repo 分类内核；需独立 ADR/规格后开 |

### 15.8 愿景阶段 0–6 与执行轨映射（保留原文阶段语义）

以下保留「阶段」作为 **能力包标签**，执行顺序以 15.3 三轨为准：

| 愿景阶段 | 执行归属 | 备注 |
|---|---|---|
| 0–1 | 已完成（维护） | 软件包演进走引擎 release gate |
| 2 | 轨 F1 + 轨 D | 工作区 + 可安装 Desktop |
| 3 | 引擎部分已完成；宿主审计 → 轨 P | 首发不过度承诺 Command 总线 |
| 4 | 轨 F2 | 与 F1 并行 |
| 5 | 轨 E B0→B1 | 全量含 CGM 在 B1 |
| 6 | 轨 D 切片 + 轨 P | 安装升级在 D；插件生态在 P |

### 15.9 原阶段任务清单（归档对照）

下列条目仍是愿景 backlog，**状态见 15.1**，实施切块见 15.4–15.7。

- **阶段 0**：产品边界、领域模型、Manifest、API 版本、双平台 CI、SDK 0.1。
- **阶段 1**：数据内核、单井控件、OpenGL、大规模曲线、平移缩放比例尺。
- **阶段 2**：主窗口、工程中心、图道面板、八类图道、模板、图头图尾、工程保存。
- **阶段 3**：拾取、Patch、Command、Undo/Redo、Transaction、曲线/地层/岩性编辑、审计、表联动。
- **阶段 4**：连井文档/画布、井序间距、层位连接、拉平、井间充填、单井连井联动、连井模板。
- **阶段 5**：长卷/大幅面 PDF、SVG、CGM、TIFF/PNG、打印、花纹字体、WYSIWYG。
- **阶段 6**：SDK 稳定、插件脚手架与管理、嵌入文档、安装升级、兼容报告、手册。

---

## 16. 验收指标

所有性能指标均应在指定硬件、指定操作系统和固定测试数据集下验证，不使用绝对化表述。

> **与 §15 的关系**：下表是愿景终点指标。**Desktop 首发**以 §15.4–15.6 轨 F/D/E 的 B0 门禁为准；表中 CGM、完整插件隔离、0.1 mm 全矩阵等属 B1 或轨 P，不得与首发混为同一检查单。

| 类别 | 验收指标 |
|---|---|
| 软件包 | 可被独立 C++ 和 PySide6 示例程序集成，不依赖桌面应用私有代码 |
| 桌面程序 | 可通过安装包独立安装、启动、升级和卸载 |
| 单井数据规模 | 单曲线支持不少于 500 万采样点 |
| 单井工程规模 | 单工程支持不少于 100 条曲线 |
| 单井图道规模 | 同一单井文档支持不少于 50 个可见图道 |
| 单井渲染 | 1920×1080 下连续平移 P95 不低于 50 FPS |
| 连井规模 | 单个连井文档支持不少于 20 口井 |
| 连井图道 | 每口井可显示不少于 5 个主要图道 |
| 连井交互 | 井序调整、层位拖动和拉平切换保持可交互响应 |
| 连井功能 | 支持井间距、层位连接、地层拉平、井间充填和跨井高亮 |
| 数据联动 | 单井地层修改能够正确通知并刷新相关连井文档 |
| 反向编辑 | 连井控制点修改能够通过事务更新对应单井数据 |
| 首次打开 | 1 GB 测试工程在指定硬件下达到约定时间阈值 |
| 内存占用 | 相同井数据被多个文档引用时不进行无控制重复加载 |
| 导出精度 | 单井和连井几何位置误差不大于 0.1 mm |
| 稳定性 | 单插件加载失败不影响桌面应用启动 |
| 撤销重做 | 单井和连井编辑操作均可撤销和重做 |
| 数据安全 | 插件不得绕过 Command/Transaction 修改工程数据 |
| 跨平台 | Windows/Linux 通过同一功能和插件契约测试 |
| SDK | 第三方开发者可独立完成图道、数据源、算法和连井扩展插件 |
| 输出 | 单井长卷和连井大幅面 PDF、SVG、CGM 通过一致性检查 |

---

## 17. 风险与应对措施

### 17.1 C++ 与 Python 生命周期风险

应对措施：

- C++ 不长期持有裸 `PyObject*`；
- 使用明确所有权规则；
- Qt 对象遵守父子对象树；
- Python 回调使用弱引用；
- 绑定层释放 GIL；
- 插件卸载前取消后台任务。

### 17.2 OpenGL 驱动差异

应对措施：

- 限制在 OpenGL 3.3 Core；
- 不使用厂商私有扩展；
- 建立 NVIDIA、AMD、Intel 测试矩阵；
- **交互路径不提供软件 OpenGL/QPainter 回退**（ADR 0033）；无合适 GPU 时明确失败并诊断，表投影与无头 SVG/PDF 等非交互路径仍可用；
- Shader 编译失败时输出明确诊断。

### 17.3 插件 ABI 风险

应对措施：

- 稳定 C ABI；
- Arrow C Data Interface；
- 版本化结构体；
- Qt 插件使用固定工具链；
- Python 插件使用 SDK 版本约束；
- CI 中运行旧插件兼容测试。

### 17.4 WYSIWYG 风险

应对措施：

- 统一物理毫米布局；
- 统一 Render Command；
- 字体嵌入；
- 屏幕和输出采用同一文本测量逻辑；
- 建立基准图件像素和几何差异测试。

### 17.5 单井与连井并行建设风险

应对措施：

- 单井和连井共享领域模型、渲染命令、图道扩展点和输出后端；
- **当前执行**允许单井工作区补齐与连井工作区 **并行**（§15 轨 F），但连井不得复制单井渲染内核；
- 单井解释成果为连井显示的上游输入；连井控制点回写经统一 Patch/撤销；
- 连井首版聚焦井列排列、层位连接、拉平和井间充填；
- 智能自动对比等高级算法作为后置扩展逐步增加；
- 每条执行轨均提供可运行的软件包或 Desktop 构建；
- 性能指标通过固定数据集逐步验证。

---

## 18. 最终技术路线

本项目最终采用“可复用软件包 + 独立桌面应用”的双层技术路线：

```text
WellPlot Desktop
+ Single-Well Workspace
+ Multi-Well Correlation Workspace
────────────────────────────────────
WellPlot Reusable Packages
+ SingleWellWidget
+ MultiWellCorrelationWidget
+ C++20 Core Kernel
+ Qt 6 / PySide6 Integration
+ Shared OpenGL Render Engine
+ Device-independent Render Commands
+ Apache Arrow Columnar Store
+ Command / Transaction / Event Services
+ Native SDK and Python SDK
+ Versioned Plugin Runtime
+ PDF / SVG / CGM / Raster Backends
```

软件包是全部专业能力的统一实现，可被独立桌面应用、第三方地质平台、Python 工具和批处理程序共同调用。桌面应用是面向最终用户的完整产品，不仅提供软件包控件的简单容器，还负责工程流程、单井与连井工作区组织、窗口布局、用户交互、插件管理、打印输出和产品化发布。

单井和连井采用同一数据源与插件体系：

- 单井负责一口井的图道绘制、编辑和解释；
- 连井负责多井排列、层位对比、地层拉平和井间地质体表达；
- 单井解释成果直接参与连井显示；
- 连井编辑通过统一命令和事务更新单井数据；
- 两类文档共享模板、图道、样式、输出和审计服务。

在本方案下，项目最终形成：

1. 一套可单独授权、集成和二次开发的测井绘图软件包；
2. 一个可独立安装运行、同时支持单井和连井的桌面程序；
3. 一套面向 C++ 与 Python 开发者的 SDK 和插件体系；
4. 一套可持续扩展到储层评价、智能解释和多井综合分析的技术底座。

